---
phase: 01-detection-and-preview
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - gesture_keys/__main__.py
  - gesture_keys/preview.py
  - tests/test_integration.py
autonomous: false
requirements:
  - DEV-01
  - DEV-02
  - DEV-03

must_haves:
  truths:
    - "Running python -m gesture_keys --preview opens a camera window with live feed and hand landmarks"
    - "Gesture label appears in bottom-left of solid bar below the feed"
    - "FPS counter appears in bottom-right of solid bar below the feed"
    - "Console prints gesture name on transitions (not every frame), including None transitions"
    - "Left hand produces no overlay or label"
    - "ESC key or window close exits the application cleanly"
    - "Startup banner shows version, camera info, config loaded, gesture count (4 lines max)"
  artifacts:
    - path: "gesture_keys/__main__.py"
      provides: "CLI entry point with argparse and main detection loop"
      exports: ["main"]
    - path: "gesture_keys/preview.py"
      provides: "OpenCV preview window rendering with bottom bar"
      exports: ["render_preview", "draw_hand_landmarks"]
    - path: "tests/test_integration.py"
      provides: "Integration test for console output"
  key_links:
    - from: "gesture_keys/__main__.py"
      to: "gesture_keys/detector.py"
      via: "CameraCapture and HandDetector instantiation"
      pattern: "CameraCapture|HandDetector"
    - from: "gesture_keys/__main__.py"
      to: "gesture_keys/classifier.py"
      via: "GestureClassifier.classify() per frame"
      pattern: "classifier\\.classify"
    - from: "gesture_keys/__main__.py"
      to: "gesture_keys/smoother.py"
      via: "GestureSmoother.update() per frame"
      pattern: "smoother\\.update"
    - from: "gesture_keys/__main__.py"
      to: "gesture_keys/preview.py"
      via: "render_preview() and draw_hand_landmarks() per frame"
      pattern: "render_preview|draw_hand_landmarks"
    - from: "gesture_keys/preview.py"
      to: "mediapipe.framework.formats.landmark_pb2"
      via: "Landmark-to-proto conversion for draw_landmarks"
      pattern: "NormalizedLandmarkList"
---

<objective>
Wire the preview window and CLI entry point, connecting all modules into the working detection loop.

Purpose: This is the integration layer -- the main loop reads frames from CameraCapture, runs HandDetector, classifies with GestureClassifier, smooths with GestureSmoother, and renders in the OpenCV preview window. This makes the phase deliverable: a user can run `python -m gesture_keys --preview` and see their gestures detected in real time.

Output: Complete working application with preview mode showing camera feed, landmarks, gesture label, and FPS counter.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-detection-and-preview/01-CONTEXT.md
@.planning/phases/01-detection-and-preview/01-RESEARCH.md
@.planning/phases/01-detection-and-preview/01-01-SUMMARY.md
@.planning/phases/01-detection-and-preview/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From gesture_keys/config.py:
```python
@dataclass
class AppConfig:
    camera_index: int
    smoothing_window: int
    gestures: dict

def load_config(path: str = "config.yaml") -> AppConfig: ...
```

From gesture_keys/classifier.py:
```python
class Gesture(Enum):
    OPEN_PALM = "open_palm"
    FIST = "fist"
    THUMBS_UP = "thumbs_up"
    PEACE = "peace"
    POINTING = "pointing"
    PINCH = "pinch"

class GestureClassifier:
    def __init__(self, thresholds: dict): ...
    def classify(self, landmarks) -> Gesture | None: ...
```

From gesture_keys/smoother.py:
```python
class GestureSmoother:
    def __init__(self, window_size: int = 3): ...
    def update(self, gesture) -> Gesture | None: ...
    def reset(self): ...
```

<!-- From Plan 02 outputs -->

From gesture_keys/detector.py:
```python
class CameraCapture:
    def __init__(self, camera_index: int = 0): ...
    def start(self) -> "CameraCapture": ...
    def read(self) -> tuple[bool, np.ndarray | None]: ...
    def stop(self): ...

class HandDetector:
    def __init__(self, model_path: str = "models/hand_landmarker.task"): ...
    def detect(self, frame: np.ndarray, timestamp_ms: int) -> list: ...
    def close(self): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preview renderer and CLI entry point</name>
  <files>gesture_keys/preview.py, gesture_keys/__main__.py, tests/test_integration.py</files>
  <action>
    1. Create `gesture_keys/preview.py`:
       - `draw_hand_landmarks(frame, hand_landmarks)`:
         - Convert Task API landmarks to `landmark_pb2.NormalizedLandmarkList` proto (RESEARCH.md Pattern 3 -- CRITICAL, direct use crashes with AttributeError)
         - Call `solutions.drawing_utils.draw_landmarks()` with HAND_CONNECTIONS, default landmark style, default connection style
         - Draw on the frame in-place
       - `render_preview(frame, gesture_name, fps)`:
         - Follow RESEARCH.md Bottom Bar Rendering pattern exactly
         - BAR_HEIGHT = 40, dark gray bar (50, 50, 50)
         - Gesture label bottom-left: `cv2.putText` at (10, 28), HERSHEY_SIMPLEX 0.7, white, thickness 2
         - FPS counter bottom-right: `f"FPS: {fps:.0f}"`, HERSHEY_SIMPLEX 0.6, green (0, 255, 0), thickness 1
         - Stack frame + bar with np.vstack
         - Show with `cv2.imshow("Gesture Keys", display)`
       - Window size is 640x480 for the camera feed per CONTEXT.md (bar adds 40px below)

    2. Create `gesture_keys/__main__.py`:
       - `parse_args()`: argparse with --preview (store_true), --config (default "config.yaml")
       - `print_banner(config)`: 4-line startup banner per CONTEXT.md:
         - Line 1: "Gesture Keys v{version}"
         - Line 2: "Camera: index {N}"
         - Line 3: "Config: {path} ({N} gestures loaded)"
         - Line 4: "Detection started..."
       - `main()`:
         - Parse args, load config, print banner
         - Setup logging: format "[%(asctime)s] %(message)s" with datefmt="%H:%M:%S" per CONTEXT.md (no milliseconds)
         - INFO level for gesture changes, DEBUG for frame-level
         - Create CameraCapture(config.camera_index).start()
         - Create HandDetector() (auto-downloads model)
         - Create GestureClassifier(config.gestures) with per-gesture thresholds
         - Create GestureSmoother(config.smoothing_window)
         - Main loop (follow RESEARCH.md Complete Main Loop Pattern):
           a. camera.read() -- skip if no frame
           b. Track FPS with time.perf_counter() (calculate rolling average or simple delta)
           c. detector.detect(frame, timestamp_ms) -- get right-hand landmarks
           d. If landmarks found: classifier.classify(landmarks), smoother.update(raw_gesture)
           e. If no landmarks: smoother.update(None)
           f. On gesture transition (current != previous): log at INFO level "Gesture: {name}" including None transitions per CONTEXT.md
           g. If --preview: draw_hand_landmarks on frame (only if landmarks found), render_preview(frame, gesture, fps)
           h. If --preview: cv2.waitKey(1), break on ESC (key 27) or cv2.getWindowProperty check for window closed
         - Cleanup: camera.stop(), detector.close(), cv2.destroyAllWindows()
         - Wrap in try/except for KeyboardInterrupt (clean shutdown)
       - `if __name__ == "__main__": main()`

    3. Create `tests/test_integration.py`:
       - `test_console_output`: Mock CameraCapture, HandDetector, and cv2 to simulate a sequence of frames with gesture transitions. Capture log output and verify:
         - Startup banner is printed
         - Gesture transitions are logged (e.g., None -> OPEN_PALM -> None)
         - Non-transition frames do NOT produce log output
       - Use unittest.mock to patch detector and camera, inject mock frames and results

    Run tests: `python -m pytest tests/ -x -v`
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -v</automated>
  </verify>
  <done>
    - preview.py renders landmarks (with proto conversion) and bottom bar with gesture label + FPS
    - __main__.py wires all modules into working detection loop
    - --preview flag controls window visibility
    - Console logs gesture transitions only (not every frame), including None
    - Startup banner is 4 lines per CONTEXT.md
    - Integration test verifies console output behavior
    - Full test suite passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete detection and preview system</name>
  <files>gesture_keys/__main__.py, gesture_keys/preview.py</files>
  <action>
    Human verifies the complete Phase 1 detection and preview system. All modules are wired: camera capture thread -> MediaPipe hand detection -> gesture classification -> majority-vote smoothing -> OpenCV preview window with landmark overlay, gesture label, and FPS counter.
  </action>
  <verify>
    1. Open a terminal in the project root
    2. Ensure venv is active and dependencies installed: `pip install -r requirements.txt`
    3. Run: `python -m gesture_keys --preview`
    4. Verify startup banner appears (4 lines: version, camera, config, "Detection started...")
    5. Verify camera preview window opens showing live webcam feed (640x480)
    6. Hold up your RIGHT hand and perform each gesture:
       - Open palm (all fingers spread) -- should show "OPEN_PALM" label
       - Fist (all fingers curled) -- should show "FIST" label
       - Thumbs up (thumb up, fingers curled) -- should show "THUMBS_UP" label
       - Peace sign (index + middle up) -- should show "PEACE" label
       - Pointing (index finger only) -- should show "POINTING" label
       - Pinch (thumb + index tips together) -- should show "PINCH" label
    7. Verify 21-landmark skeleton with connections is drawn on your hand
    8. Verify solid dark bar below feed shows gesture label (bottom-left) and FPS (bottom-right)
    9. Verify FPS counter updates and shows reasonable number (expect 15-30+ FPS)
    10. Hold up your LEFT hand only -- verify NO landmark overlay and NO gesture label appears
    11. Check console output: gesture names should appear on transitions only (not every frame)
    12. Press ESC to exit -- verify clean shutdown (no errors)

    NOTE on handedness (RESEARCH.md Open Question 1): If your right hand is being ignored and left hand is detected, the handedness labels may be mirrored. Report this -- the filter in detector.py can be swapped from "Right" to "Left".
  </verify>
  <done>
    - All 6 gestures correctly detected and labeled for right hand
    - Left hand silently ignored
    - Landmarks drawn, FPS visible, bottom bar rendered
    - Console shows transitions only
    - Clean exit on ESC
    - User types "approved" or describes issues
  </done>
</task>

</tasks>

<verification>
1. Full test suite: `python -m pytest tests/ -v` -- all green
2. Manual verification: `python -m gesture_keys --preview` shows working detection
3. Phase success criteria from ROADMAP.md:
   - Running with --preview opens camera window with live feed and landmarks
   - Each of 6 gestures detected and labeled for right hand
   - Left hand is ignored
   - Console output prints gesture names on transitions
   - FPS counter visible in preview window
</verification>

<success_criteria>
- `python -m gesture_keys --preview` opens camera preview at 640x480 with bottom bar
- All 6 gestures classified and labeled correctly for right hand
- Left hand silently ignored (no overlay, no label)
- 21-landmark skeleton drawn on detected right hand
- FPS counter updates in bottom-right of bar
- Console shows gesture transitions with [HH:MM:SS] timestamps
- ESC exits cleanly
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-detection-and-preview/01-03-SUMMARY.md`
</output>
