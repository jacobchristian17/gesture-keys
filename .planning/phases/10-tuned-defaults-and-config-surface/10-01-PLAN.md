---
phase: 10-tuned-defaults-and-config-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gesture_keys/config.py
  - gesture_keys/debounce.py
  - gesture_keys/smoother.py
  - gesture_keys/__main__.py
  - gesture_keys/tray.py
  - config.yaml
  - tests/test_config.py
  - tests/test_debounce.py
autonomous: true
requirements:
  - TUNE-01
  - TUNE-02

must_haves:
  truths:
    - "AppConfig code defaults are activation_delay=0.15, cooldown_duration=0.3, smoothing_window=2"
    - "GestureDebouncer code defaults are activation_delay=0.15, cooldown_duration=0.3"
    - "GestureSmoother code default is window_size=2"
    - "config.yaml ships with smoothing_window=2, activation_delay=0.15, cooldown_duration=0.3"
    - "settling_frames is configurable in swipe section of config.yaml"
    - "settling_frames is passed from config to SwipeDetector in both loops"
    - "settling_frames updates on hot-reload in both loops"
  artifacts:
    - path: "gesture_keys/config.py"
      provides: "Updated defaults + swipe_settling_frames field"
      contains: "swipe_settling_frames"
    - path: "gesture_keys/smoother.py"
      provides: "Updated default window_size=2"
      contains: "window_size: int = 2"
    - path: "gesture_keys/debounce.py"
      provides: "Updated default activation_delay=0.15, cooldown_duration=0.3"
      contains: "activation_delay: float = 0.15"
    - path: "config.yaml"
      provides: "Tuned default values + settling_frames"
      contains: "settling_frames"
  key_links:
    - from: "gesture_keys/config.py"
      to: "gesture_keys/__main__.py"
      via: "config.swipe_settling_frames passed to SwipeDetector constructor"
      pattern: "settling_frames.*config"
    - from: "gesture_keys/config.py"
      to: "gesture_keys/tray.py"
      via: "config.swipe_settling_frames passed to SwipeDetector constructor"
      pattern: "settling_frames.*config"
---

<objective>
Update code defaults to proven tuned values and make settling_frames configurable.

Purpose: Fresh installs get responsive defaults (activation ~0.15s, cooldown ~0.3s, smoothing ~2) and power users can tune settling_frames via config.yaml.
Output: Updated defaults in all source files + tests, settling_frames as a config surface parameter.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tuned-defaults-and-config-surface/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From gesture_keys/config.py:
```python
@dataclass
class AppConfig:
    camera_index: int = 0
    smoothing_window: int = 3          # -> change to 2
    activation_delay: float = 0.4      # -> change to 0.15
    cooldown_duration: float = 0.8     # -> change to 0.3
    # ... other fields ...
    # ADD: swipe_settling_frames: int = 3

def load_config(path: str = "config.yaml") -> AppConfig:
    # detection parsing uses .get() with defaults matching AppConfig
    # swipe parsing at line 118-125 already parses swipe section
    # ADD: swipe_settling_frames=int(swipe.get("settling_frames", 3))
```

From gesture_keys/debounce.py:
```python
class GestureDebouncer:
    def __init__(self, activation_delay: float = 0.4, cooldown_duration: float = 0.8):
        # -> change defaults to 0.15 and 0.3
```

From gesture_keys/smoother.py:
```python
class GestureSmoother:
    def __init__(self, window_size: int = 3):
        # -> change default to 2
```

From gesture_keys/swipe.py (SwipeDetector already has settling_frames param):
```python
class SwipeDetector:
    def __init__(self, ..., settling_frames: int = 3):
    # Already has settling_frames property setter for hot-reload
```

Hot-reload blocks (__main__.py line 262-287, tray.py line 255-278):
- Both directly set swipe_detector._settling_frames_remaining = 0 on reload
- Need to ADD: swipe_detector.settling_frames = new_config.swipe_settling_frames
- Both pass settling_frames default (3) via SwipeDetector constructor -- need to pass config value

Test files with hardcoded old defaults:
- tests/test_config.py line 179: assert config.activation_delay == 0.4
- tests/test_config.py line 185: assert config.cooldown_duration == 0.8
- tests/test_config.py line 189-190: assert 0.4 and 0.8
- tests/test_config.py line 28: assert config.smoothing_window == 3 (reads from config.yaml)
- tests/test_debounce.py: uses explicit params in constructors, no default assertions to break
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update code defaults and config.yaml to tuned values (TUNE-01)</name>
  <files>gesture_keys/config.py, gesture_keys/debounce.py, gesture_keys/smoother.py, config.yaml, tests/test_config.py, tests/test_debounce.py</files>
  <behavior>
    - AppConfig() with no args has smoothing_window=2, activation_delay=0.15, cooldown_duration=0.3
    - GestureDebouncer() with no args has _activation_delay=0.15, _cooldown_duration=0.3
    - GestureSmoother() with no args has _window_size=2
    - load_config() on minimal YAML (no detection section) returns activation_delay=0.15, cooldown_duration=0.3, smoothing_window=2
    - load_config() on config.yaml returns smoothing_window=2 (config.yaml updated from 30 to 2)
    - Existing tests that specify explicit timing values (0.6, 1.2 etc) still pass unchanged
  </behavior>
  <action>
    1. Update tests/test_config.py:
       - TestAppConfigTimingFields.test_activation_delay_default_when_missing: change assert from 0.4 to 0.15
       - TestAppConfigTimingFields.test_cooldown_duration_default_when_missing: change assert from 0.8 to 0.3
       - TestAppConfigTimingFields.test_default_config_has_timing_fields: change asserts from 0.4/0.8 to 0.15/0.3
       - TestLoadConfigDefault.test_smoothing_window_default: change assert from 3 to 2 (config.yaml will have 2)

    2. Run tests -- expect RED (old defaults in code + config.yaml)

    3. Update gesture_keys/config.py AppConfig dataclass:
       - smoothing_window: int = 2 (was 3)
       - activation_delay: float = 0.15 (was 0.4)
       - cooldown_duration: float = 0.3 (was 0.8)
       - Update load_config() fallback defaults to match: .get("smoothing_window", 2), .get("activation_delay", 0.15), .get("cooldown_duration", 0.3)

    4. Update gesture_keys/debounce.py GestureDebouncer.__init__ defaults:
       - activation_delay: float = 0.15 (was 0.4)
       - cooldown_duration: float = 0.3 (was 0.8)

    5. Update gesture_keys/smoother.py GestureSmoother.__init__ default:
       - window_size: int = 2 (was 3)

    6. Update config.yaml:
       - smoothing_window: 2 (was 30)
       - activation_delay: 0.15 (was 0.2)
       - cooldown_duration: 0.3 (already 0.3, no change needed)

    7. Run tests -- expect GREEN
  </action>
  <verify>
    <automated>python -m pytest tests/test_config.py tests/test_debounce.py -x -q</automated>
  </verify>
  <done>All code defaults match tuned values (0.15/0.3/2), config.yaml updated, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add settling_frames to config surface (TUNE-02)</name>
  <files>gesture_keys/config.py, gesture_keys/__main__.py, gesture_keys/tray.py, config.yaml, tests/test_config.py</files>
  <behavior>
    - AppConfig() with no args has swipe_settling_frames=3
    - load_config() on YAML with swipe.settling_frames=5 returns swipe_settling_frames=5
    - load_config() on YAML with no settling_frames returns swipe_settling_frames=3
    - config.yaml has settling_frames: 3 in swipe section
  </behavior>
  <action>
    1. Add tests to tests/test_config.py:
       - In TestSwipeConfig: test_default_appconfig_settling_frames asserting AppConfig().swipe_settling_frames == 3
       - In TestSwipeConfig: test_settling_frames_parsed_from_config with YAML containing swipe.settling_frames: 5, assert config.swipe_settling_frames == 5
       - In TestSwipeConfig: test_missing_settling_frames_defaults with YAML containing swipe section but no settling_frames, assert config.swipe_settling_frames == 3

    2. Run tests -- expect RED

    3. Update gesture_keys/config.py:
       - Add field to AppConfig: swipe_settling_frames: int = 3
       - In load_config(), add to the return: swipe_settling_frames=int(swipe.get("settling_frames", 3))

    4. Run tests -- expect GREEN

    5. Update gesture_keys/__main__.py:
       - In SwipeDetector constructor call (line 165-170), add: settling_frames=config.swipe_settling_frames
       - In hot-reload block (after line 270 swipe_detector._settling_frames_remaining = 0), add: swipe_detector.settling_frames = new_config.swipe_settling_frames

    6. Update gesture_keys/tray.py:
       - In SwipeDetector constructor call (line 169-174), add: settling_frames=config.swipe_settling_frames
       - In hot-reload block (after line 263 swipe_detector._settling_frames_remaining = 0), add: swipe_detector.settling_frames = new_config.swipe_settling_frames

    7. Update config.yaml -- add settling_frames: 3 in swipe section (after cooldown: 0.5 line):
       ```yaml
       swipe:
         cooldown: 0.5
         settling_frames: 3
       ```

    8. Run full test suite
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <done>settling_frames is configurable in config.yaml, parsed into AppConfig, passed to SwipeDetector in both loops, and updates on hot-reload</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -v` -- full suite green
- `python -c "from gesture_keys.config import AppConfig; c = AppConfig(); print(c.smoothing_window, c.activation_delay, c.cooldown_duration, c.swipe_settling_frames)"` prints `2 0.15 0.3 3`
- `python -c "from gesture_keys.debounce import GestureDebouncer; d = GestureDebouncer(); print(d._activation_delay, d._cooldown_duration)"` prints `0.15 0.3`
- `python -c "from gesture_keys.smoother import GestureSmoother; s = GestureSmoother(); print(s._window_size)"` prints `2`
</verification>

<success_criteria>
- All code defaults match tuned values: activation_delay=0.15, cooldown_duration=0.3, smoothing_window=2
- config.yaml ships with tuned values
- settling_frames is configurable in swipe section of config.yaml
- settling_frames wired through both __main__.py and tray.py loops (constructor + hot-reload)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-tuned-defaults-and-config-surface/10-01-SUMMARY.md`
</output>
