---
phase: 21-orchestrator-refactor
plan: 02
type: tdd
wave: 2
depends_on: ["21-01"]
files_modified:
  - gesture_keys/orchestrator.py
  - tests/test_orchestrator.py
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-04

must_haves:
  truths:
    - "Orchestrator emits MOVING_FIRE when a confirmed gesture is active and motion_state.moving is True with a direction"
    - "Orchestrator emits SEQUENCE_FIRE when two gestures fire in order matching a registered sequence within the time window"
    - "Sequence window defaults to 0.5s and is configurable via constructor"
    - "MOVING_FIRE is not emitted during ACTIVATING or COOLDOWN states"
    - "Both standalone FIRE and SEQUENCE_FIRE emit when a sequence completes"
  artifacts:
    - path: "gesture_keys/orchestrator.py"
      provides: "Orchestrator with MOVING_FIRE and SEQUENCE_FIRE signals"
      contains: "MOVING_FIRE"
    - path: "tests/test_orchestrator.py"
      provides: "Tests for all new signal behaviors"
      contains: "class TestMovingFire"
  key_links:
    - from: "gesture_keys/orchestrator.py"
      to: "gesture_keys/motion.py"
      via: "MotionState import for update() parameter"
      pattern: "from gesture_keys\\.motion import MotionState"
    - from: "gesture_keys/orchestrator.py"
      to: "gesture_keys/trigger.py"
      via: "Direction for MOVING_FIRE signal, Gesture for sequence tracking"
      pattern: "from gesture_keys\\.trigger import Direction"
---

<objective>
Add MOVING_FIRE and SEQUENCE_FIRE signals to the orchestrator, with motion_state parameter on update() and sequence tracking via constructor-provided sequence definitions.

Purpose: Enable the orchestrator to emit signals for gesture+motion and gesture-sequence triggers, completing the tri-state signal model.
Output: Orchestrator with 5 action types (FIRE, HOLD_START, HOLD_END, MOVING_FIRE, SEQUENCE_FIRE) and full test coverage.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-orchestrator-refactor/21-CONTEXT.md
@.planning/phases/21-orchestrator-refactor/21-RESEARCH.md
@.planning/phases/21-orchestrator-refactor/21-01-SUMMARY.md

@gesture_keys/orchestrator.py
@tests/test_orchestrator.py
@gesture_keys/trigger.py
@gesture_keys/motion.py

<interfaces>
<!-- Types the executor needs -->

From gesture_keys/motion.py:
```python
@dataclass(frozen=True)
class MotionState:
    moving: bool
    direction: Optional[Direction] = None
```

From gesture_keys/trigger.py:
```python
class Direction(Enum):
    LEFT = "left"
    RIGHT = "right"
    UP = "up"
    DOWN = "down"

class Gesture(Enum):
    # Various gesture values (from classifier.py, re-exported)
```

Target OrchestratorAction enum (after this plan):
```python
class OrchestratorAction(Enum):
    FIRE = "fire"
    HOLD_START = "hold_start"
    HOLD_END = "hold_end"
    MOVING_FIRE = "moving_fire"
    SEQUENCE_FIRE = "sequence_fire"
```

Target OrchestratorSignal (after this plan):
```python
class OrchestratorSignal(NamedTuple):
    action: OrchestratorAction
    gesture: Gesture
    direction: Optional[Direction] = None
    second_gesture: Optional[Gesture] = None
```

Target update() signature:
```python
def update(self, gesture: Optional[Gesture], timestamp: float, *,
           motion_state: Optional[MotionState] = None) -> OrchestratorResult:
```

Target new constructor params:
```python
sequence_definitions: set[tuple[Gesture, Gesture]] | None = None,
sequence_window: float = 0.5,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MOVING_FIRE signal with TDD</name>
  <files>gesture_keys/orchestrator.py, tests/test_orchestrator.py</files>
  <behavior>
    - MOVING_FIRE emitted alongside FIRE on tap when motion_state.moving is True with direction
    - MOVING_FIRE emitted each frame during ACTIVE(HOLD) when motion_state.moving is True with direction
    - MOVING_FIRE NOT emitted during ACTIVATING (gesture not yet confirmed)
    - MOVING_FIRE NOT emitted during COOLDOWN
    - MOVING_FIRE NOT emitted when motion_state is None
    - MOVING_FIRE NOT emitted when motion_state.moving is False
    - MOVING_FIRE NOT emitted when motion_state.direction is None (moving but no direction)
    - MOVING_FIRE signal carries correct gesture and direction fields
    - motion_state=None does not crash any state
    - Existing FIRE, HOLD_START, HOLD_END behavior unchanged
  </behavior>
  <action>
RED phase -- Write tests first in a new `TestMovingFire` class in test_orchestrator.py:
- `test_moving_fire_on_tap_fire`: Setup tap gesture, activate past delay with motion_state=MotionState(moving=True, direction=Direction.LEFT). Expect signals list contains both FIRE and MOVING_FIRE. MOVING_FIRE signal has correct gesture and direction.
- `test_moving_fire_during_hold`: Setup hold gesture, activate past delay (HOLD_START emitted), then continue holding with motion_state moving. Expect MOVING_FIRE each frame.
- `test_no_moving_fire_during_activating`: Gesture present but below activation delay, motion_state moving. Expect no MOVING_FIRE.
- `test_no_moving_fire_during_cooldown`: Gesture in cooldown, motion_state moving. Expect no MOVING_FIRE.
- `test_no_moving_fire_when_motion_state_none`: Active gesture, motion_state=None. Expect no MOVING_FIRE.
- `test_no_moving_fire_when_not_moving`: Active gesture, motion_state=MotionState(moving=False). Expect no MOVING_FIRE.
- `test_moving_fire_has_correct_direction`: Verify signal.direction matches motion_state.direction.
- `test_motion_state_none_no_crash`: Call update() with motion_state=None in every state (idle, activating, active, cooldown). No crashes.

Also update `TestTypeDefinitions`:
- Add test for MOVING_FIRE enum value
- Update signal creation test to use Direction instead of SwipeDirection
- Add test for OrchestratorSignal with 4 fields (including second_gesture)

Run tests -- they should FAIL (RED).

GREEN phase -- Implement in orchestrator.py:
1. Add `from gesture_keys.motion import MotionState` import
2. Add `MOVING_FIRE = "moving_fire"` to OrchestratorAction
3. Add `second_gesture: Optional[Gesture] = None` to OrchestratorSignal (needed for SEQUENCE_FIRE in Task 2, but add now to avoid breaking the NamedTuple later)
4. Add `motion_state: Optional[MotionState] = None` keyword param to update()
5. Pass motion_state through to handlers that need it: `_handle_activating` and `_handle_active`/`_handle_hold`
6. In `_handle_activating` when FIRE is emitted (tap mode): check motion_state, if moving with direction, append MOVING_FIRE signal
7. In `_handle_hold` each frame: check motion_state, if moving with direction, append MOVING_FIRE signal
8. Do NOT emit MOVING_FIRE in _handle_idle or _handle_cooldown

Run tests -- they should PASS (GREEN).
  </action>
  <verify>
    <automated>cd C:/Users/wsenr/repos/source/gesture-keys && python -m pytest tests/test_orchestrator.py -k "moving_fire or type_def" -x -q</automated>
  </verify>
  <done>MOVING_FIRE tests pass, existing tap/hold tests still pass, motion_state parameter accepted in update()</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SEQUENCE_FIRE signal and sequence tracking with TDD</name>
  <files>gesture_keys/orchestrator.py, tests/test_orchestrator.py</files>
  <behavior>
    - SEQUENCE_FIRE emitted when gesture B fires within sequence_window after gesture A fired, for registered (A, B) pair
    - SEQUENCE_FIRE NOT emitted for unregistered pairs
    - SEQUENCE_FIRE NOT emitted for reversed pair (B, A) when only (A, B) is registered
    - SEQUENCE_FIRE NOT emitted when time between fires exceeds sequence_window
    - SEQUENCE_FIRE signal has gesture=A (first), second_gesture=B (second)
    - Both standalone FIRE for B and SEQUENCE_FIRE are emitted on sequence completion
    - sequence_window defaults to 0.5s
    - sequence_window is configurable via constructor
    - Empty sequence_definitions means no sequences tracked
    - Stale fire timestamps do not trigger false sequences (window expiry check)
  </behavior>
  <action>
RED phase -- Write tests in a new `TestSequenceFire` class:
- `test_sequence_fire_on_matching_pair`: Register (FIST, PEACE). Fire FIST (tap past delay), then fire PEACE within 0.5s. Expect SEQUENCE_FIRE with gesture=FIST, second_gesture=PEACE alongside PEACE's standalone FIRE.
- `test_no_sequence_fire_for_unregistered_pair`: Register (FIST, PEACE). Fire FIST then OPEN_PALM. No SEQUENCE_FIRE.
- `test_no_sequence_fire_for_reversed_pair`: Register (FIST, PEACE). Fire PEACE then FIST. No SEQUENCE_FIRE.
- `test_no_sequence_fire_outside_window`: Register (FIST, PEACE). Fire FIST, wait 0.6s, fire PEACE. No SEQUENCE_FIRE.
- `test_sequence_fire_at_window_boundary`: Register (FIST, PEACE) with 0.5s window. Fire FIST at t=1.0, fire PEACE at t=1.5. Should still fire (<=).
- `test_sequence_window_configurable`: Set sequence_window=1.0. Register (FIST, PEACE). Fire FIST, wait 0.8s, fire PEACE. SEQUENCE_FIRE emitted.
- `test_sequence_window_default_half_second`: Constructor with no sequence_window param. Verify 0.5s window works.
- `test_sequence_fire_includes_both_gestures`: Verify signal has gesture=first, second_gesture=second.
- `test_standalone_fire_also_emitted`: When sequence completes, signals list contains FIRE for B AND SEQUENCE_FIRE.
- `test_no_sequence_tracking_with_empty_definitions`: No sequence_definitions param. Fire two gestures. No SEQUENCE_FIRE.

Also update `TestConstructor.test_accepts_all_config_params` to include `sequence_definitions` and `sequence_window` params.

Run tests -- they should FAIL (RED).

GREEN phase -- Implement in orchestrator.py:
1. Add constructor params: `sequence_definitions: set[tuple[Gesture, Gesture]] | None = None, sequence_window: float = 0.5`
2. Add instance vars: `self._sequence_definitions = sequence_definitions or set()`, `self._sequence_window = sequence_window`, `self._last_fire_time: dict[Gesture, float] = {}`
3. Add `SEQUENCE_FIRE = "sequence_fire"` to OrchestratorAction
4. Add `_check_sequences(self, fired_gesture: Gesture, timestamp: float, signals: list)` method per RESEARCH.md Pattern 2:
   - For each (first, second) in sequence_definitions: if second == fired_gesture and first in last_fire_time and timestamp - last_fire_time[first] <= sequence_window: append SEQUENCE_FIRE signal with gesture=first, second_gesture=fired_gesture
   - Update last_fire_time[fired_gesture] = timestamp
5. Call `_check_sequences()` after every FIRE signal emission in `_handle_activating` (tap mode fires)
6. Also call after HOLD_START if that counts as a "fire" for sequences -- actually per user decision, sequence tracking is based on FIRE signal only (gesture A "completed" means it emitted FIRE). HOLD_START is not FIRE, so do NOT trigger sequence on HOLD_START.
7. Add `self._last_fire_time = {}` to `reset()` method

Run tests -- they should PASS (GREEN).

REFACTOR: Review both MOVING_FIRE and SEQUENCE_FIRE implementations for clean integration. Ensure no unnecessary complexity.
  </action>
  <verify>
    <automated>cd C:/Users/wsenr/repos/source/gesture-keys && python -m pytest tests/test_orchestrator.py -x -q</automated>
  </verify>
  <done>All orchestrator tests pass including new MOVING_FIRE and SEQUENCE_FIRE tests, sequence_window defaults to 0.5s, constructor accepts sequence_definitions and sequence_window, full test suite green</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_orchestrator.py -x -q` -- all orchestrator tests pass
2. `python -m pytest tests/test_orchestrator.py -k "moving_fire" -x -q` -- MOVING_FIRE tests pass
3. `python -m pytest tests/test_orchestrator.py -k "sequence_fire" -x -q` -- SEQUENCE_FIRE tests pass
4. `python -m pytest tests/ -x -q` -- full test suite passes
5. Verify OrchestratorAction has exactly 5 values: FIRE, HOLD_START, HOLD_END, MOVING_FIRE, SEQUENCE_FIRE
6. Verify OrchestratorSignal has 4 fields: action, gesture, direction, second_gesture
7. Verify update() accepts motion_state keyword parameter
8. Verify constructor accepts sequence_definitions and sequence_window
</verification>

<success_criteria>
- MOVING_FIRE emitted during FIRE (tap + moving) and during ACTIVE(HOLD) + moving
- MOVING_FIRE NOT emitted during ACTIVATING or COOLDOWN
- SEQUENCE_FIRE emitted when registered pair fires within window
- SEQUENCE_FIRE NOT emitted for unregistered, reversed, or expired pairs
- Sequence window defaults to 0.5s, configurable via constructor
- Both standalone FIRE and SEQUENCE_FIRE emitted on sequence completion
- All existing non-swipe tests still pass
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/21-orchestrator-refactor/21-02-SUMMARY.md`
</output>
