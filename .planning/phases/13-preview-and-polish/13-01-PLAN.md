---
phase: 13-preview-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gesture_keys/preview.py
  - gesture_keys/__main__.py
  - tests/test_preview.py
autonomous: false
requirements:
  - PRV-01

must_haves:
  truths:
    - "Preview overlay displays which hand (Left or Right) is currently active"
    - "Switching hands in front of the camera updates the hand indicator in real time"
    - "Hand indicator is visible but does not obscure gesture label, debounce state, or FPS"
  artifacts:
    - path: "gesture_keys/preview.py"
      provides: "render_preview with handedness parameter"
      contains: "handedness"
    - path: "gesture_keys/__main__.py"
      provides: "handedness passed to render_preview call"
      contains: "handedness="
    - path: "tests/test_preview.py"
      provides: "Tests for hand indicator rendering"
  key_links:
    - from: "gesture_keys/__main__.py"
      to: "gesture_keys/preview.py"
      via: "render_preview(handedness=prev_handedness)"
      pattern: "render_preview.*handedness="
---

<objective>
Add a hand indicator to the preview overlay so users can see which hand (Left/Right) is currently active, and verify the full left-hand workflow end-to-end.

Purpose: Completes v1.3 Left Hand Support milestone by giving users visual confirmation of active hand.
Output: Updated preview.py with hand label, updated __main__.py call site, test coverage, user-verified overlay.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-preview-and-polish/13-CONTEXT.md

<interfaces>
<!-- Key interfaces the executor needs -->

From gesture_keys/preview.py (current signature):
```python
def render_preview(frame, gesture_name, fps, debounce_state=None):
    """Render frame with solid bottom bar showing gesture label and FPS."""
```

Bottom bar layout (BAR_HEIGHT=40, y=28 text baseline):
- Left (x=10): gesture label, white, 0.7 scale
- Center: debounce state, color-coded, 0.5 scale
- Right (x=w-textwidth-10): FPS, green, 0.6 scale

From gesture_keys/__main__.py (call site at line ~409):
```python
render_preview(frame, gesture_label, fps, debounce_state=debouncer.state.value)
```

Variable `prev_handedness` holds "Left" or "Right" (or None before first detection) and is available in scope at the render call site.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add handedness parameter to render_preview and display hand indicator</name>
  <files>gesture_keys/preview.py, tests/test_preview.py, gesture_keys/__main__.py</files>
  <behavior>
    - Test 1: render_preview with handedness="Right" renders "R" text on the bar (check bar pixel region is not all background gray)
    - Test 2: render_preview with handedness="Left" renders "L" text on the bar
    - Test 3: render_preview with handedness=None renders no hand indicator (backward compat)
    - Test 4: render_preview with no handedness kwarg works without error (default None)
  </behavior>
  <action>
1. Create tests/test_preview.py with the behavior tests above. Use numpy to create a dummy 640x480 BGR frame. Call render_preview with handedness values and inspect the returned/displayed bar region for non-background pixels in the hand indicator area. Mock cv2.imshow to capture the display frame instead of showing a window.

2. In gesture_keys/preview.py, add `handedness=None` optional kwarg to `render_preview()`. When handedness is not None:
   - Display a compact hand label between the gesture label and the debounce state. Position: x ~ 1/4 of bar width (roughly after gesture label area, before center debounce state).
   - Format: "L" for Left, "R" for Right -- short single letter to keep the bar uncluttered.
   - Color: (255, 200, 0) cyan-ish blue for Left, (0, 200, 255) orange-ish for Right -- distinct from existing debounce state colors and FPS green.
   - Font scale 0.6, thickness 1 -- slightly smaller than gesture label but readable.
   - Use cv2.putText on the bar numpy array, same y=28 baseline as other elements.

3. In gesture_keys/__main__.py, update the render_preview call at line ~409 to pass `handedness=prev_handedness`:
   ```python
   render_preview(frame, gesture_label, fps, debounce_state=debouncer.state.value, handedness=prev_handedness)
   ```

This keeps the existing kwarg-based interface pattern (optional params with None default) established in phase 08 for debounce_state.
  </action>
  <verify>
    <automated>python -m pytest tests/test_preview.py -x -v</automated>
  </verify>
  <done>render_preview shows "L" or "R" indicator when handedness is provided, no indicator when None, all tests pass, __main__.py passes prev_handedness to render call</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify hand indicator in live preview</name>
  <files>gesture_keys/__main__.py</files>
  <action>
User visually verifies the hand indicator in the live preview window. No code changes -- this is a human verification checkpoint.

What was built: Hand indicator in preview overlay showing which hand (L/R) is active, updating in real time when switching hands.

How to verify:
1. Run: python -m gesture_keys --preview
2. Hold your LEFT hand in front of the camera
3. Verify the bottom bar shows "L" indicator (between gesture label and debounce state)
4. Switch to your RIGHT hand
5. Verify the indicator updates to "R" in real time
6. Verify the indicator does not overlap or obscure the gesture label, debounce state, or FPS counter
7. Verify gestures still fire correctly with both hands

Resume signal: Type "approved" or describe any issues with indicator placement, visibility, or behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x</automated>
  </verify>
  <done>User confirms hand indicator is visible, correctly shows L/R, updates on hand switch, and does not interfere with existing overlay elements</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_preview.py -x -v` -- all hand indicator tests pass
- `python -m pytest tests/ -x` -- full test suite still passes (no regressions)
- Visual confirmation: hand indicator visible and updates on hand switch
</verification>

<success_criteria>
1. Preview overlay shows "L" when left hand is active
2. Preview overlay shows "R" when right hand is active
3. Switching hands updates the indicator immediately (next frame)
4. Indicator does not overlap existing bar elements
5. No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/13-preview-and-polish/13-01-SUMMARY.md`
</output>
