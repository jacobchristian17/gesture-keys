---
phase: 17-activation-gate
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - config.yaml
autonomous: false
requirements: [ACTV-01, ACTV-02]

must_haves:
  truths:
    - "Default config.yaml has activation_gate section with enabled: false"
    - "User can enable activation gate by setting enabled: true in config.yaml"
    - "Activation gate works end-to-end: scout/peace arms, other gestures fire only while armed"
  artifacts:
    - path: "config.yaml"
      provides: "Documented activation_gate section with enabled, gestures, duration fields"
      contains: "activation_gate"
  key_links:
    - from: "config.yaml"
      to: "gesture_keys/config.py"
      via: "load_config() parses activation_gate section"
      pattern: "activation_gate"
---

<objective>
Add activation_gate section to config.yaml and verify end-to-end activation gate behavior with human testing.

Purpose: Ship the activation gate feature with documented config and verified real-world behavior.
Output: Updated config.yaml with activation_gate section, human-verified activation gate behavior.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-activation-gate/17-01-SUMMARY.md

@config.yaml
@gesture_keys/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add activation_gate section to config.yaml</name>
  <files>config.yaml</files>
  <action>Add an `activation_gate` section to config.yaml with documented fields:

```yaml
# Activation gate: arm/disarm gesture detection with an activation gesture.
# When enabled, gestures only fire actions while the gate is armed.
# The activation gesture arms the gate for the configured duration.
activation_gate:
  enabled: false          # Set to true to enable activation gating (default: bypass, v1.x behavior)
  gestures:               # Gestures that arm the gate (perform any of these to activate)
    - scout
    - peace
  duration: 3.0           # Seconds the gate stays armed after activation
```

Place it after the existing top-level sections (after `left_gestures` or at the end of the file). Use comment style consistent with existing config.yaml sections.</action>
  <verify>
    <automated>python -c "from gesture_keys.config import load_config; c = load_config(); print(f'gate_enabled={c.activation_gate_enabled}, gestures={c.activation_gate_gestures}, duration={c.activation_gate_duration}')"</automated>
  </verify>
  <done>config.yaml has activation_gate section, load_config() parses it correctly, enabled defaults to false (bypass mode active)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Activation gate feature: gesture-based arm/disarm gating with configurable bypass. Default is bypass mode (disabled), preserving v1.x behavior. When enabled, scout/peace gesture arms the system for 3 seconds, and other gestures only fire during that armed window.</what-built>
  <how-to-verify>
    1. **Verify bypass mode (default):** Run gesture-keys normally. All gestures should fire as before (no behavior change). This confirms ACTV-02.

    2. **Enable activation gate:** Edit config.yaml, set `activation_gate.enabled: true`. Save.

    3. **Verify gate arms on activation gesture (ACTV-01):**
       - Make a non-activation gesture (e.g., fist). It should NOT fire its mapped key.
       - Make a scout or peace gesture. This arms the gate for 3 seconds.
       - Within 3 seconds, make a fist gesture. It SHOULD fire its mapped key.
       - Wait 3+ seconds without activating. Make a fist gesture again. It should NOT fire.

    4. **Verify activation gesture consumed (ACTV-03):**
       - When you make the scout gesture to arm the gate, its mapped key (if any) should NOT fire. Only the gate arming should happen.

    5. **Verify re-arming extends window:**
       - Arm the gate with scout. Wait 2 seconds. Arm again with peace. The 3-second window should restart from the second activation.

    6. **Disable gate:** Set `activation_gate.enabled: false` in config.yaml. Hot-reload should restore normal bypass behavior immediately.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
python -m pytest tests/ -x -q
</verification>

<success_criteria>
- config.yaml has documented activation_gate section
- Bypass mode (default) preserves exact v1.x behavior
- Enabled mode gates gestures behind activation gesture
- Activation gesture is consumed (doesn't fire mapped key)
- Human verification confirms real-world behavior
</success_criteria>

<output>
After completion, create `.planning/phases/17-activation-gate/17-02-SUMMARY.md`
</output>
