---
phase: 20-config-loader-for-actions
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - gesture_keys/config.py
  - tests/test_config.py
  - config.yaml
autonomous: true
requirements:
  - CONF-04
  - CONF-05

must_haves:
  truths:
    - "System derives gesture_modes dict from action trigger states (static->tap, holding->hold_key, moving->tap)"
    - "System derives per-gesture cooldown map from action cooldown overrides"
    - "System derives gate bypass list from action bypass_gate flags"
    - "load_config() with actions: section returns AppConfig with derived fields populated"
    - "load_config() with both actions: and gestures: raises ValueError (no mixed formats)"
    - "load_config() without actions: falls back to existing gestures:/swipe: parsing"
    - "config.yaml uses new actions: format and loads correctly"
  artifacts:
    - path: "gesture_keys/config.py"
      provides: "derive_from_actions() + updated load_config()"
      contains: "derive_from_actions"
    - path: "config.yaml"
      provides: "Converted config using actions: format"
      contains: "actions:"
    - path: "tests/test_config.py"
      provides: "Tests for derivation and integration"
      contains: "TestDeriveFromActions"
  key_links:
    - from: "gesture_keys/config.py:load_config"
      to: "gesture_keys/config.py:parse_actions"
      via: "actions path in load_config"
      pattern: "parse_actions"
    - from: "gesture_keys/config.py:load_config"
      to: "gesture_keys/config.py:derive_from_actions"
      via: "populating AppConfig fields"
      pattern: "derive_from_actions"
---

<objective>
Wire action parsing into load_config(), derive orchestrator inputs from actions, convert config.yaml to actions: format, and enforce mutual exclusion with old config sections.

Purpose: Complete the config loading pipeline so downstream phases (21-23) can consume derived gesture_modes, cooldowns, and bypass sets from action definitions. Remove need for separate gestures:/swipe: config.
Output: Working load_config() with actions: path, converted config.yaml, derived orchestrator inputs.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-config-loader-for-actions/20-CONTEXT.md
@.planning/phases/20-config-loader-for-actions/20-01-SUMMARY.md

@gesture_keys/config.py
@gesture_keys/trigger.py
@gesture_keys/action.py
@gesture_keys/pipeline.py
@config.yaml
@tests/test_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive orchestrator inputs from parsed actions</name>
  <files>gesture_keys/config.py, tests/test_config.py</files>
  <behavior>
    - derive_from_actions(actions: list[ActionEntry]) returns a dict/dataclass with:
      - gesture_modes: dict[str, str] mapping gesture_name -> "tap"/"hold_key" (from trigger state: static->tap, holding->hold_key, moving->tap, sequence->tap)
      - gesture_cooldowns: dict[str, float] for actions with cooldown overrides
      - activation_gate_bypass: list[str] of gesture names with bypass_gate=True
      - action_maps built via build_action_maps-style logic: per-hand Action dicts keyed by gesture_name
      - For hand:"both", action appears in both right and left maps
      - For hand:"left", action appears only in left map
      - For hand:"right", action appears only in right map
    - Test: 3 actions (static tap, holding hold, one with cooldown) -> correct gesture_modes, cooldowns
    - Test: action with bypass_gate -> appears in bypass list
    - Test: hand scoping produces correct per-hand maps
  </behavior>
  <action>
    Create derive_from_actions() function in config.py. Takes list[ActionEntry] (from plan 01), returns a DerivedConfig dataclass (or dict) with gesture_modes, gesture_cooldowns, activation_gate_bypass, right_actions (dict[str, Action]), left_actions (dict[str, Action]).

    For each ActionEntry:
    - Infer fire_mode from trigger state: TriggerState.STATIC -> FireMode.TAP, TriggerState.HOLDING -> FireMode.HOLD_KEY, TriggerState.MOVING -> FireMode.TAP. For SequenceTrigger, always FireMode.TAP.
    - Call parse_key_string(entry.key) to get modifiers + key.
    - Build Action(key_string=entry.key, fire_mode=..., gesture_name=entry.name, modifiers=..., key=...).
    - Place into right/left/both maps based on entry.hand.
    - Collect cooldown overrides into gesture_cooldowns dict.
    - Collect bypass_gate gesture names into activation_gate_bypass list.

    NOTE: gesture_swipe_mappings and compound action maps are NOT derived here -- moving triggers will use a new signal path in Phase 22, not the old compound fire. For now, moving-trigger actions go into the per-hand maps keyed by a composite key or a separate moving_actions map. Use the approach: separate maps for static/holding actions vs moving actions vs sequence actions, so ActionResolver (Phase 22) can look them up by signal type.

    Create a DerivedConfig frozen dataclass:
    ```python
    @dataclass(frozen=True)
    class DerivedConfig:
        gesture_modes: dict[str, str]
        gesture_cooldowns: dict[str, float]
        activation_gate_bypass: list[str]
        right_actions: dict[str, Action]
        left_actions: dict[str, Action]
    ```
  </action>
  <verify>
    <automated>pytest tests/test_config.py -x -v -k "TestDeriveFromActions"</automated>
  </verify>
  <done>derive_from_actions() produces correct gesture_modes, cooldowns, bypass lists, and per-hand action maps from ActionEntry list</done>
</task>

<task type="auto">
  <name>Task 2: Wire into load_config and convert config.yaml</name>
  <files>gesture_keys/config.py, config.yaml, tests/test_config.py</files>
  <action>
    **Part A: Update load_config() with actions: path**

    In load_config(), after reading raw YAML:
    1. Check for mutual exclusion: if raw has both "actions" and ("gestures" or "swipe"), raise ValueError("config.yaml cannot contain both 'actions:' and 'gestures:'/'swipe:' sections. Use only 'actions:' (new format) or 'gestures:'+'swipe:' (legacy format).")
    2. If "actions" key exists in raw:
       - Call parse_actions(raw["actions"]) to get list[ActionEntry]
       - Call derive_from_actions(actions) to get DerivedConfig
       - Populate AppConfig fields from DerivedConfig (gesture_modes, gesture_cooldowns, activation_gate_bypass)
       - Do NOT require "gestures" section (remove that validation when actions: is present)
       - Still parse camera, detection, distance, activation_gate sections as before
       - Store parsed actions on AppConfig (add new field `actions: list[ActionEntry]` with default empty list)
    3. If "actions" not present, fall back to existing gestures:/swipe: parsing (current code, unchanged)

    **Part B: Convert config.yaml to actions: format**

    Convert the repo's config.yaml from old format to new actions: format. Map each gesture entry to an action:

    ```yaml
    actions:
      open_palm_switch:
        trigger: "open_palm:static"
        key: win+tab
        threshold: 0.7

      fist_hold:
        trigger: "fist:holding"
        key: space
        threshold: 0.7

      thumbs_up_tap:
        trigger: "thumbs_up:static"
        key: enter
        threshold: 0.9

      pointing_switch:
        trigger: "pointing:static"
        key: alt+tab
        threshold: 0.7

      peace_desktop_right:
        trigger: "peace:static"
        key: win+ctrl+right
        threshold: 0.7
        bypass_gate: true

      scout_desktop_left:
        trigger: "scout:static"
        key: win+ctrl+left
        threshold: 0.7
        bypass_gate: true

      pinch_minimize:
        trigger: "pinch:static"
        key: win+down
        threshold: 0.06

      swipe_left:
        trigger: "open_palm:moving:right"
        key: right

      swipe_right:
        trigger: "open_palm:moving:left"
        key: left

      swipe_up:
        trigger: "open_palm:moving:up"
        key: up

      swipe_down:
        trigger: "open_palm:moving:down"
        key: down
    ```

    Keep camera, detection, distance, activation_gate sections unchanged. Remove old gestures: and swipe: sections.

    Note for swipe mappings: the old swipe section was global (any gesture), but the new format ties direction to a specific gesture. Use open_palm as the gesture for swipe-equivalent actions (matches common usage). Preserve the swipe detection settings (settling_frames, min_velocity, etc.) in the detection: section or a new motion: section for Phase 23 to consume.

    Actually -- per CONTEXT.md, old swipe settings (settling_frames, min_velocity, etc.) are SwipeDetector params that become MotionDetector params in Phase 23. Keep them in a `motion:` section in config.yaml so they're available later, but don't parse them in this phase.

    **Part C: Update tests**

    - Update TestLoadConfigDefault to work with new config.yaml format (actions: instead of gestures:)
    - Add TestLoadConfigActions class: test that load_config with actions: section returns correct AppConfig
    - Add test for mutual exclusion error
    - Add test for fallback to gestures: when actions: absent
    - Existing tests that use tmp_path with gestures: format should still pass (they test fallback path)
  </action>
  <verify>
    <automated>pytest tests/test_config.py -x -v</automated>
  </verify>
  <done>
    - load_config() with actions: section returns AppConfig with derived gesture_modes, cooldowns, bypass
    - load_config() with both actions: and gestures: raises ValueError
    - load_config() without actions: falls back to legacy parsing
    - config.yaml uses actions: format and all existing tests adapted to pass
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
pytest tests/test_config.py -x -v
pytest tests/ -x --timeout=30
</verification>

<success_criteria>
- config.yaml converted to actions: format with all current mappings preserved
- load_config() parses actions: and derives gesture_modes, cooldowns, bypass sets
- Mutual exclusion prevents mixed old+new config
- Fallback to gestures:/swipe: works when actions: absent
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-config-loader-for-actions/20-02-SUMMARY.md`
</output>
