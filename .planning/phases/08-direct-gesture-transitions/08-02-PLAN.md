---
phase: 08-direct-gesture-transitions
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - gesture_keys/preview.py
  - gesture_keys/__main__.py
autonomous: true
requirements:
  - TRANS-03

must_haves:
  truths:
    - "Preview window displays current debounce state (IDLE/ACTIVATING/COOLDOWN) in the bottom bar"
    - "State indicator is color-coded: gray for IDLE, yellow for ACTIVATING, orange for COOLDOWN, green for FIRED"
    - "Existing preview functionality (gesture label, FPS, hand skeleton) is unchanged"
  artifacts:
    - path: "gesture_keys/preview.py"
      provides: "Debounce state indicator in render_preview"
      contains: "debounce_state"
    - path: "gesture_keys/__main__.py"
      provides: "Passes debouncer.state to render_preview"
      contains: "debounce_state="
  key_links:
    - from: "gesture_keys/__main__.py"
      to: "gesture_keys/preview.py:render_preview"
      via: "debounce_state kwarg"
      pattern: "render_preview.*debounce_state"
    - from: "gesture_keys/preview.py:render_preview"
      to: "cv2.putText"
      via: "state text rendering"
      pattern: "putText.*state"
---

<objective>
Add debounce state indicator to the preview overlay and wire it through the detection loop.

Purpose: Users need visual feedback showing why a gesture has or hasn't fired. Displaying IDLE/ACTIVATING/COOLDOWN in the preview bar lets users understand the debounce timing and debug gesture issues.

Output: Modified `preview.py` with state indicator, updated `__main__.py` to pass state through.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-direct-gesture-transitions/08-CONTEXT.md
@.planning/phases/08-direct-gesture-transitions/08-RESEARCH.md

@gesture_keys/preview.py
@gesture_keys/__main__.py
@gesture_keys/debounce.py

<interfaces>
<!-- From Plan 01 output -- debouncer now has _cooldown_gesture -->
From gesture_keys/debounce.py:
```python
class DebounceState(Enum):
    IDLE = "IDLE"
    ACTIVATING = "ACTIVATING"
    FIRED = "FIRED"
    COOLDOWN = "COOLDOWN"

class GestureDebouncer:
    @property
    def state(self) -> DebounceState  # .value gives string "IDLE", "ACTIVATING", etc.
```

From gesture_keys/preview.py (current signature to modify):
```python
def render_preview(frame, gesture_name, fps):
    # Current: no debounce_state parameter
```

From gesture_keys/__main__.py (current call at line 291):
```python
render_preview(frame, gesture_label, fps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add debounce state indicator to preview overlay</name>
  <files>gesture_keys/preview.py</files>
  <action>
    Modify `render_preview` to accept an optional `debounce_state` keyword argument (default None) and display it centered in the bottom bar with color coding.

    1. Update signature: `def render_preview(frame, gesture_name, fps, debounce_state=None):`
    2. After the FPS text rendering block (line 104), add debounce state rendering:
       - Only render if debounce_state is not None
       - Color map (BGR): IDLE=(128,128,128) gray, ACTIVATING=(0,255,255) yellow, COOLDOWN=(0,128,255) orange, FIRED=(0,255,0) green
       - Center the state text horizontally in the bar
       - Use cv2.FONT_HERSHEY_SIMPLEX, scale 0.5, thickness 1
       - Position vertically at y=28 (same baseline as other text)
    3. Use keyword argument with default None so ALL existing callers work unchanged (critical -- see Pitfall 3 in research)
    4. Update docstring to document the new parameter

    Do NOT modify any other part of render_preview. Do NOT change BAR_HEIGHT, colors, or layout of existing elements.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -v</automated>
  </verify>
  <done>render_preview accepts optional debounce_state kwarg, displays color-coded state text centered in bottom bar, all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire debounce state through detection loop</name>
  <files>gesture_keys/__main__.py</files>
  <action>
    Update the render_preview call in `run_preview_mode` to pass the current debounce state.

    1. At line 291, change:
       `render_preview(frame, gesture_label, fps)`
       to:
       `render_preview(frame, gesture_label, fps, debounce_state=debouncer.state.value)`

    2. `debouncer` is already in scope at this point in the function (created earlier in run_preview_mode). `debouncer.state` returns a `DebounceState` enum, `.value` gives the string ("IDLE", "ACTIVATING", etc.).

    Do NOT modify tray.py -- it has no preview rendering, and the debounce logic change from Plan 01 applies automatically via the shared GestureDebouncer class.
    Do NOT change any other part of __main__.py.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -v</automated>
  </verify>
  <done>Preview mode displays IDLE/ACTIVATING/COOLDOWN/FIRED state in the bottom bar, color-coded, centered between gesture label and FPS</done>
</task>

</tasks>

<verification>
Full test suite passes: `python -m pytest tests/ -x -v`
Manual spot-check (optional): Run `python -m gesture_keys --preview`, observe state indicator changes as gestures are performed.
</verification>

<success_criteria>
- render_preview accepts debounce_state kwarg without breaking existing callers
- Preview bottom bar shows color-coded state text (IDLE=gray, ACTIVATING=yellow, COOLDOWN=orange, FIRED=green)
- State indicator is centered horizontally in the bar
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-direct-gesture-transitions/08-02-SUMMARY.md`
</output>
