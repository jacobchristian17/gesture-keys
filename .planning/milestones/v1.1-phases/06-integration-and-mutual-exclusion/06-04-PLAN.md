---
phase: 06-integration-and-mutual-exclusion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [config.yaml, tests/test_config.py]
autonomous: true
gap_closure: true
requirements: [INT-01]

must_haves:
  truths:
    - "User can see and edit distance gating settings in config.yaml"
    - "Distance gating is enabled by default so the feature works out of the box"
  artifacts:
    - path: "config.yaml"
      provides: "distance section with enabled and min_hand_size"
      contains: "distance:"
    - path: "tests/test_config.py"
      provides: "Updated test expecting distance enabled in default config"
  key_links:
    - from: "config.yaml"
      to: "gesture_keys/config.py"
      via: "load_config reads distance.enabled and distance.min_hand_size"
      pattern: "distance\\.get"
---

<objective>
Add the missing `distance:` section to config.yaml so users can configure distance gating.

Purpose: UAT test 4 failed because distance gating has no user-facing config -- the backend fully supports it but config.yaml lacks the section, so distance defaults to disabled with hardcoded values invisible to the user.
Output: config.yaml with distance section, updated test expectation.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add distance section to config.yaml and update test</name>
  <files>config.yaml, tests/test_config.py</files>
  <action>
1. Add a `distance:` section to config.yaml between the `detection:` and `gestures:` sections (logical grouping). The section should contain:
   ```yaml
   distance:
     enabled: true
     min_hand_size: 0.15
   ```
   Use `enabled: true` so the feature works out of the box. Use `min_hand_size: 0.15` which matches the existing default in config.py (AppConfig.min_hand_size).

2. In tests/test_config.py, update the test `test_default_config_yaml_has_distance_disabled` (line ~229):
   - Rename to `test_default_config_yaml_has_distance_enabled`
   - Change assertion from `assert config.distance_enabled is False` to `assert config.distance_enabled is True`
   - Also assert `config.min_hand_size == 0.15` to verify the value round-trips

This is the ONLY change needed. The backend (config.py load_config) already reads `distance.enabled` and `distance.min_hand_size` from YAML. Both detection loops (__main__.py and tray.py) already pass these values to DistanceFilter. The gap is purely the missing YAML section.
  </action>
  <verify>
    <automated>cd C:/Users/wsenr/repos/source/gesture-keys && python -m pytest tests/test_config.py -x -q</automated>
  </verify>
  <done>config.yaml contains distance: section with enabled: true and min_hand_size: 0.15. All config tests pass including the updated default-config test.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_config.py -x -q` -- all config tests pass
- `grep -A2 "distance:" config.yaml` shows enabled: true and min_hand_size: 0.15
- `python -c "from gesture_keys.config import load_config; c = load_config(); print(c.distance_enabled, c.min_hand_size)"` prints `True 0.15`
</verification>

<success_criteria>
config.yaml has a visible, editable distance: section. load_config() reads it correctly. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-and-mutual-exclusion/06-04-SUMMARY.md`
</output>
