---
phase: 06-integration-and-mutual-exclusion
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - gesture_keys/swipe.py
  - tests/test_swipe.py
  - tests/test_integration_mutual_exclusion.py
autonomous: true
requirements:
  - INT-01
  - INT-02

must_haves:
  truths:
    - "SwipeDetector.is_swiping returns True when state is ARMED or COOLDOWN"
    - "SwipeDetector.is_swiping returns False when state is IDLE"
    - "SwipeDetector.reset() clears buffer and returns to IDLE state"
    - "SwipeDetector.reset() preserves COOLDOWN state (does not interrupt active cooldown)"
  artifacts:
    - path: "gesture_keys/swipe.py"
      provides: "is_swiping property and reset() method"
      contains: "def is_swiping"
    - path: "tests/test_swipe.py"
      provides: "Unit tests for is_swiping and reset"
      contains: "TestSwipeIsSwiping"
    - path: "tests/test_integration_mutual_exclusion.py"
      provides: "Integration tests for mutual exclusion scenarios"
      contains: "test_swipe_suppresses_static"
  key_links:
    - from: "gesture_keys/swipe.py"
      to: "_SwipeState"
      via: "is_swiping checks ARMED/COOLDOWN membership"
      pattern: "_SwipeState\\.ARMED.*_SwipeState\\.COOLDOWN"
---

<objective>
Add the `is_swiping` property and `reset()` method to SwipeDetector, with full test coverage including integration tests for mutual exclusion scenarios.

Purpose: These two additions to SwipeDetector are the foundation that Plan 02 wires into the main loops. The is_swiping property exposes the swipe state machine to the main loop for static gesture suppression. The reset() method allows distance gating to cleanly reset swipe state on range transitions.

Output: Updated SwipeDetector with is_swiping + reset(), unit tests, integration test file.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-and-mutual-exclusion/06-RESEARCH.md

<interfaces>
<!-- Existing SwipeDetector internals the executor needs -->

From gesture_keys/swipe.py:
```python
class _SwipeState(Enum):
    IDLE = "IDLE"
    ARMED = "ARMED"
    COOLDOWN = "COOLDOWN"

class SwipeDetector:
    # Internal state fields:
    self._buffer: deque[tuple[float, float, float]]
    self._state = _SwipeState.IDLE
    self._prev_speed: float = 0.0
    self._cooldown_start: float = 0.0
    self._armed_direction: Optional[SwipeDirection] = None
```

From tests/test_swipe.py:
```python
# Test helpers already available:
def _make_wrist_landmarks(x, y) -> list
def _swipe_sequence(detector, positions, start_time=0.0, dt=0.033) -> list
def _generate_swipe_positions(start, end, steps=8) -> list
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD -- is_swiping property and reset() method</name>
  <files>gesture_keys/swipe.py, tests/test_swipe.py</files>
  <behavior>
    - TestSwipeIsSwiping::test_idle_not_swiping: Fresh detector, is_swiping == False
    - TestSwipeIsSwiping::test_armed_is_swiping: After feeding positions that cross velocity/displacement thresholds (enter ARMED), is_swiping == True
    - TestSwipeIsSwiping::test_cooldown_is_swiping: After a complete swipe fires (enters COOLDOWN), is_swiping == True
    - TestSwipeIsSwiping::test_back_to_idle_not_swiping: After cooldown expires and state returns to IDLE, is_swiping == False
    - TestSwipeReset::test_reset_clears_buffer: After feeding positions, reset() makes buffer empty (next update with landmarks returns None due to < 3 entries)
    - TestSwipeReset::test_reset_returns_to_idle: When in ARMED state, reset() transitions to IDLE
    - TestSwipeReset::test_reset_preserves_cooldown: When in COOLDOWN state, reset() does NOT change state to IDLE (cooldown must expire naturally)
    - TestSwipeReset::test_reset_clears_armed_direction: After entering ARMED, reset() clears _armed_direction to None
  </behavior>
  <action>
    RED: Add TestSwipeIsSwiping and TestSwipeReset test classes to tests/test_swipe.py. Run tests -- they must fail (is_swiping and reset don't exist yet).

    To get the detector into ARMED state for testing: use _generate_swipe_positions with start=(0.7, 0.5) end=(0.2, 0.5) but only feed the first 5-6 positions (enough to cross thresholds but not decelerate). Check detector._state == _SwipeState.ARMED to confirm the setup worked before asserting is_swiping.

    To get into COOLDOWN: feed a full swipe sequence that fires, then check is_swiping.

    GREEN: Add to SwipeDetector class in gesture_keys/swipe.py:

    1. `is_swiping` read-only property: returns `self._state in (_SwipeState.ARMED, _SwipeState.COOLDOWN)`

    2. `reset()` method:
       - Clear self._buffer
       - If self._state != _SwipeState.COOLDOWN: set self._state = _SwipeState.IDLE
       - Set self._prev_speed = 0.0
       - Set self._armed_direction = None

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>python -m pytest tests/test_swipe.py::TestSwipeIsSwiping tests/test_swipe.py::TestSwipeReset -x -v</automated>
  </verify>
  <done>is_swiping property returns True for ARMED/COOLDOWN, False for IDLE. reset() clears buffer/state but preserves COOLDOWN. All 8 new tests pass alongside existing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD -- Integration tests for mutual exclusion scenarios</name>
  <files>tests/test_integration_mutual_exclusion.py</files>
  <behavior>
    - test_swipe_suppresses_static: Simulate a swipe motion. During ARMED/COOLDOWN frames, verify is_swiping is True (static pipeline would be suppressed). After cooldown expires, is_swiping is False (static pipeline resumes).
    - test_held_pose_does_not_trigger_swipe: Feed 20 frames of identical wrist position (simulating held pose). SwipeDetector returns None every frame and is_swiping stays False.
    - test_distance_reset_clears_swipe: Feed positions that enter ARMED state, then call reset() (simulating distance out-of-range). Verify state returns to IDLE and buffer is cleared. Next update with new landmarks does not false-fire from stale data.
    - test_swipe_to_pose_transition: Complete a swipe (enters COOLDOWN), wait for cooldown to expire, then feed stable positions. Verify is_swiping transitions from True to False cleanly.
  </behavior>
  <action>
    RED: Create tests/test_integration_mutual_exclusion.py with the 4 test functions above. Import SwipeDetector, SwipeDirection, and reuse _make_wrist_landmarks, _swipe_sequence, _generate_swipe_positions helpers (import from tests.test_swipe or duplicate the helpers).

    These tests validate the contract that Plan 02 relies on when wiring the main loop. They do NOT test the main loop itself -- they test that SwipeDetector's is_swiping and reset() behave correctly in realistic multi-frame scenarios.

    For test_held_pose_does_not_trigger_swipe: feed the same (0.5, 0.5) position 20 times at dt=0.033. All results should be None, is_swiping should be False throughout.

    For test_distance_reset_clears_swipe: feed 5 frames of a fast swipe to enter ARMED, call reset(), then feed 2 frames of a new position. The 2 frames should not cause any fire (insufficient buffer after reset).

    GREEN: These tests should already pass from Task 1's implementation. Run to confirm.
  </action>
  <verify>
    <automated>python -m pytest tests/test_integration_mutual_exclusion.py -x -v</automated>
  </verify>
  <done>4 integration tests pass. Full test suite (python -m pytest tests/ -x) is green.</done>
</task>

</tasks>

<verification>
python -m pytest tests/ -x
All existing tests still pass. New tests for is_swiping, reset(), and integration scenarios all green.
</verification>

<success_criteria>
- SwipeDetector has is_swiping property (read-only, bool)
- SwipeDetector has reset() method
- 8+ new unit tests in test_swipe.py
- 4 integration tests in test_integration_mutual_exclusion.py
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-and-mutual-exclusion/06-01-SUMMARY.md`
</output>
