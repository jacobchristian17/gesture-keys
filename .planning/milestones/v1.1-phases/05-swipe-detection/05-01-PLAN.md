---
phase: 05-swipe-detection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - gesture_keys/swipe.py
  - tests/test_swipe.py
  - gesture_keys/config.py
  - tests/test_config.py
autonomous: true
requirements: [SWIPE-01, SWIPE-02, SWIPE-03, SWIPE-04]

must_haves:
  truths:
    - "SwipeDetector detects left, right, up, down swipes from wrist position sequences"
    - "Swipe fires once on deceleration after velocity/displacement thresholds are met"
    - "Cooldown prevents double-firing; buffer clears on fire and hand loss"
    - "Swipe detection only reads WRIST landmark, not hand pose"
    - "Config swipe section parsed into AppConfig with enabled flag, thresholds, and key mappings"
  artifacts:
    - path: "gesture_keys/swipe.py"
      provides: "SwipeDetector class, SwipeDirection enum"
      exports: ["SwipeDetector", "SwipeDirection"]
    - path: "tests/test_swipe.py"
      provides: "Tests for all swipe detection behaviors"
      min_lines: 80
    - path: "gesture_keys/config.py"
      provides: "AppConfig with swipe fields, load_config parsing swipe section"
      contains: "swipe_enabled"
    - path: "tests/test_config.py"
      provides: "TestSwipeConfig class testing swipe config parsing"
      contains: "TestSwipeConfig"
  key_links:
    - from: "gesture_keys/swipe.py"
      to: "collections.deque"
      via: "Rolling position buffer with maxlen"
      pattern: "deque.*maxlen"
    - from: "gesture_keys/config.py"
      to: "gesture_keys/swipe.py"
      via: "AppConfig swipe fields configure SwipeDetector parameters"
      pattern: "swipe_min_velocity|swipe_cooldown"
---

<objective>
Implement SwipeDetector class with velocity-based directional swipe detection and extend AppConfig to parse swipe configuration from YAML.

Purpose: Core swipe detection algorithm and config support -- prerequisite for pipeline integration in Plan 02.
Output: gesture_keys/swipe.py (SwipeDetector + SwipeDirection), updated config.py (swipe fields + parsing), full test coverage.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-swipe-detection/05-RESEARCH.md

@gesture_keys/config.py
@gesture_keys/distance.py
@gesture_keys/debounce.py
@tests/test_config.py
@tests/test_distance.py

<interfaces>
<!-- Existing patterns the executor must follow -->

From gesture_keys/config.py:
```python
@dataclass
class AppConfig:
    camera_index: int = 0
    smoothing_window: int = 3
    activation_delay: float = 0.4
    cooldown_duration: float = 0.8
    gestures: dict[str, dict[str, Any]] = field(default_factory=dict)
    distance_enabled: bool = False
    min_hand_size: float = 0.15

def load_config(path: str = "config.yaml") -> AppConfig:
```

From gesture_keys/distance.py (pattern to follow for property setters):
```python
class DistanceFilter:
    def __init__(self, min_hand_size: float = 0.15, enabled: bool = True) -> None:
    @property
    def enabled(self) -> bool:
    @enabled.setter
    def enabled(self, value: bool) -> None:
```

From gesture_keys/debounce.py (state machine pattern to follow):
```python
class DebounceState(Enum):
    IDLE = "IDLE"
    ACTIVATING = "ACTIVATING"
    FIRED = "FIRED"
    COOLDOWN = "COOLDOWN"
```

From gesture_keys/keystroke.py:
```python
def parse_key_string(key_string: str) -> tuple[list[Key], Union[Key, str]]:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SwipeDetector class with TDD</name>
  <files>gesture_keys/swipe.py, tests/test_swipe.py</files>
  <behavior>
    - Test: Left swipe detected from rightward-to-leftward wrist movement (decreasing x, stable y) exceeding thresholds
    - Test: Right swipe detected from leftward-to-rightward wrist movement (increasing x, stable y)
    - Test: Up swipe detected from downward-to-upward wrist movement (decreasing y -- MediaPipe inverted Y)
    - Test: Down swipe detected from upward-to-downward wrist movement (increasing y)
    - Test: Diagonal movement rejected when axis_ratio not met (abs_dx ~= abs_dy)
    - Test: No fire when displacement below min_displacement (jitter-level movement)
    - Test: No fire when velocity below min_velocity (slow repositioning)
    - Test: Fire occurs on deceleration (speed drops below prev_speed while ARMED)
    - Test: Cooldown blocks new swipes for cooldown_duration seconds
    - Test: After cooldown expires, next swipe fires immediately (no return-to-rest)
    - Test: Buffer clears when landmarks is None (hand lost), state resets to IDLE
    - Test: Buffer clears on fire (prevents stale data after cooldown)
    - Test: Works with any landmark data -- only reads index 0 (WRIST)
    - Test: Fewer than 3 buffer entries returns None (insufficient data)
  </behavior>
  <action>
    RED phase: Create tests/test_swipe.py with test classes:
    - TestSwipeDirectionClassification: Tests for all 4 directions, diagonal rejection
    - TestSwipeThresholds: Tests for min_velocity, min_displacement filtering
    - TestSwipeFireTiming: Tests for deceleration-based firing
    - TestSwipeCooldown: Tests for cooldown blocking and re-arm
    - TestSwipeBufferLifecycle: Tests for buffer clear on None landmarks and on fire
    - TestSwipePoseIndependence: Test that only WRIST (index 0) is accessed

    Use a helper to create mock landmark sequences: a simple class with .x and .y attributes at index 0.
    Generate position sequences with known velocities by spacing positions evenly over time.

    GREEN phase: Create gesture_keys/swipe.py with:
    - SwipeDirection enum: SWIPE_LEFT, SWIPE_RIGHT, SWIPE_UP, SWIPE_DOWN (values "swipe_left" etc.)
    - SwipeDetector class following the research Pattern 1 (velocity + deceleration firing):
      - __init__(buffer_size=6, min_velocity=0.4, min_displacement=0.08, axis_ratio=2.0, cooldown_duration=0.5)
      - update(landmarks, timestamp) -> SwipeDirection | None
      - _classify_direction(dx, dy, abs_dx, abs_dy) -> SwipeDirection | None
      - Property setters for all parameters (for hot-reload): min_velocity, min_displacement, axis_ratio, cooldown_duration, enabled
      - State machine: IDLE -> ARMED -> COOLDOWN (3 states, not 4 like debouncer)
      - Rolling deque(maxlen=buffer_size) storing (x, y, timestamp) tuples
      - Buffer cleared on None landmarks AND on entering COOLDOWN
      - Deceleration firing: transition from ARMED to COOLDOWN when speed < prev_speed

    IMPORTANT: MediaPipe Y-axis is inverted -- dy < 0 means upward movement. Comment this in _classify_direction.

    REFACTOR: Ensure logging follows project pattern (transition-only logging via gesture_keys logger).
  </action>
  <verify>
    <automated>python -m pytest tests/test_swipe.py -x -v</automated>
  </verify>
  <done>All swipe detection tests pass. SwipeDetector correctly identifies 4 cardinal directions, fires on deceleration, respects cooldown, clears buffer on hand loss and fire, rejects jitter/diagonal/slow movement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swipe config parsing and key mapping</name>
  <files>gesture_keys/config.py, tests/test_config.py</files>
  <behavior>
    - Test: AppConfig has swipe_enabled, swipe_cooldown, swipe_min_velocity, swipe_min_displacement, swipe_axis_ratio, swipe_mappings fields with correct defaults
    - Test: load_config parses swipe section with all threshold params
    - Test: load_config parses swipe direction sub-keys (swipe_left, swipe_right, swipe_up, swipe_down) into swipe_mappings dict
    - Test: Missing swipe section results in swipe_enabled=False and empty swipe_mappings
    - Test: Swipe section present with at least one direction mapping sets swipe_enabled=True
    - Test: Swipe section present but no direction mappings sets swipe_enabled=False
    - Test: Existing config loading (camera, detection, gestures, distance) is unaffected
  </behavior>
  <action>
    RED phase: Add TestSwipeConfig class to tests/test_config.py with tests for:
    - Default AppConfig swipe field values (enabled=False, cooldown=0.5, min_velocity=0.4, min_displacement=0.08, axis_ratio=2.0, mappings={})
    - Full swipe config parsing from YAML dict
    - Missing swipe section handling
    - Swipe enabled logic (has direction mappings = enabled)

    GREEN phase: Update gesture_keys/config.py:
    - Add to AppConfig dataclass (after min_hand_size):
      swipe_enabled: bool = False
      swipe_cooldown: float = 0.5
      swipe_min_velocity: float = 0.4
      swipe_min_displacement: float = 0.08
      swipe_axis_ratio: float = 2.0
      swipe_mappings: dict[str, str] = field(default_factory=dict)
    - In load_config(), parse swipe section following distance pattern:
      swipe = raw.get("swipe", {})
      Extract cooldown, min_velocity, min_displacement, axis_ratio with defaults
      Extract swipe_left/right/up/down sub-keys with "key" field into swipe_mappings dict
      swipe_enabled = bool(swipe) and len(swipe_mappings) > 0
    - Pattern: missing swipe section = disabled (follows distance section pattern)

    The swipe_mappings dict maps direction name to key string: {"swipe_left": "alt+left", ...}
    Actual parse_key_string() call happens at integration time (Plan 02), not in load_config.
  </action>
  <verify>
    <automated>python -m pytest tests/test_config.py -x -v</automated>
  </verify>
  <done>AppConfig has all swipe fields with correct defaults. load_config correctly parses swipe YAML section including direction mappings. Missing section = disabled. Existing config tests still pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_swipe.py tests/test_config.py -x -v` -- all swipe and config tests pass
- `python -m pytest tests/ -x` -- full suite passes (no regressions)
- SwipeDirection.SWIPE_LEFT.value == "swipe_left" (matches config key names)
- AppConfig().swipe_enabled == False (safe default)
</verification>

<success_criteria>
- SwipeDetector detects all 4 cardinal directions from wrist position sequences
- Deceleration-based fire timing prevents premature fires
- Cooldown prevents double-fires, re-arms without return-to-rest
- Buffer lifecycle correct (clears on hand loss and fire)
- Config parsing handles present, missing, and partial swipe sections
- All existing tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/05-swipe-detection/05-01-SUMMARY.md`
</output>
