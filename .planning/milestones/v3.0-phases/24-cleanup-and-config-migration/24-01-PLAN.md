---
phase: 24-cleanup-and-config-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gesture_keys/swipe.py
  - tests/test_swipe.py
  - tests/test_integration_mutual_exclusion.py
  - gesture_keys/pipeline.py
  - gesture_keys/__main__.py
  - gesture_keys/config.py
  - config.yaml
  - tests/test_config.py
  - tests/test_pipeline.py
autonomous: true
requirements: [CLNP-01, CLNP-02, CLNP-03]

must_haves:
  truths:
    - "swipe.py and test_swipe.py do not exist in the project"
    - "No file in the project imports from gesture_keys.swipe"
    - "config.yaml has only actions: format, no gestures: or swipe: sections"
    - "config.py has no legacy gestures/swipe parsing functions or AppConfig swipe fields"
    - "All tests pass with only the new actions: code path"
  artifacts:
    - path: "gesture_keys/config.py"
      provides: "Config loading with actions-only path, no legacy code"
      contains: "def load_config"
    - path: "config.yaml"
      provides: "Actions-format config file"
      contains: "actions:"
    - path: "tests/test_config.py"
      provides: "Config tests for actions-only path"
  key_links:
    - from: "gesture_keys/pipeline.py"
      to: "gesture_keys/config.py"
      via: "sequence_window field (renamed from swipe_window)"
      pattern: "sequence_window"
    - from: "gesture_keys/__main__.py"
      to: "gesture_keys/config.py"
      via: "action count for banner (replaces config.gestures)"
      pattern: "config\\.actions"
---

<objective>
Delete all legacy swipe code and config, leaving a clean codebase with only the tri-state actions: model.

Purpose: Final cleanup of v3.0 milestone -- remove dead code paths, simplify config, and eliminate technical debt from the swipe-to-motion migration.
Output: Clean codebase with no swipe.py, no legacy gestures:/swipe: config path, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cleanup-and-config-migration/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete swipe files and remove swipe references from pipeline and tests</name>
  <files>
    gesture_keys/swipe.py
    tests/test_swipe.py
    tests/test_integration_mutual_exclusion.py
    gesture_keys/pipeline.py
    gesture_keys/__main__.py
    tests/test_pipeline.py
  </files>
  <read_first>
    gesture_keys/swipe.py
    tests/test_swipe.py
    tests/test_integration_mutual_exclusion.py
    gesture_keys/pipeline.py
    gesture_keys/__main__.py
    tests/test_pipeline.py
  </read_first>
  <action>
1. **Delete files entirely:**
   - `gesture_keys/swipe.py` -- legacy SwipeDetector, no longer imported by pipeline
   - `tests/test_swipe.py` -- tests for deleted SwipeDetector
   - `tests/test_integration_mutual_exclusion.py` -- tests swipe/static mutual exclusion using SwipeDetector; all legacy concepts

2. **Edit `gesture_keys/pipeline.py`:**
   - Remove the `SWIPE_WINDOW = "SWIPE_WINDOW"` enum value from `DebounceState` (line 49). Keep only: IDLE, ACTIVATING, FIRED, COOLDOWN, HOLDING.
   - Rename `config.swipe_window` to `config.sequence_window` on line 177 where it is passed as `sequence_window=config.swipe_window` to GestureOrchestrator. After Task 2 renames the AppConfig field, this will read `sequence_window=config.sequence_window`.
   - Remove the comment `# Legacy, no longer produced by orchestrator` from the DebounceState class.
   - On line 150-155, change the threshold extraction from `config.gestures.items()` to extract thresholds from `config.actions` (list of ActionEntry). Replace:
     ```python
     thresholds = {
         name: settings.get("threshold", 0.7)
         for name, settings in config.gestures.items()
         if isinstance(settings, dict)
     }
     ```
     With:
     ```python
     thresholds = {
         entry.name: entry.threshold if entry.threshold is not None else 0.7
         for entry in config.actions
     }
     ```
     This extracts the threshold from ActionEntry.threshold (which is Optional[float], None means default 0.7).
   - Remove the comment on line 209 referencing SwipeDetector: `# Motion detection (continuous per-frame state, replaces SwipeDetector)` -- change to `# Motion detection (continuous per-frame state)`.

3. **Edit `gesture_keys/__main__.py`:**
   - Line 35: Change help text from `"Enable debug logging (shows every frame's gesture, state, swipe)"` to `"Enable debug logging (shows every frame's gesture and state)"`.
   - Line 64: Change `gesture_count = len(config.gestures)` to `gesture_count = len(config.actions)`.
   - Line 67: Change `({gesture_count} gestures loaded)` to `({gesture_count} actions loaded)`.

4. **Edit `tests/test_pipeline.py`:**
   - In `_make_mock_config` (around line 163-185): Remove `config.gestures` dict (line 168-170) and `config.swipe_window` (line 175). Add `config.sequence_window = 0.5` and ensure `config.actions = []` remains.
   - In assertions around lines 154-156: Remove `assert not hasattr(pipeline, '_pre_swipe_gesture')` and `assert not hasattr(pipeline, '_compound_swipe_suppress_until')` since these reference swipe concepts. Keep the `_was_swiping` and `_debouncer` assertions if present since they verify non-swipe legacy cleanup.
  </action>
  <verify>
    <automated>cd C:/Users/wsenr/repos/source/gesture-keys && python -m pytest tests/test_pipeline.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `gesture_keys/swipe.py` does not exist
    - `tests/test_swipe.py` does not exist
    - `tests/test_integration_mutual_exclusion.py` does not exist
    - grep -r "from gesture_keys.swipe" gesture_keys/ tests/ returns no matches (excluding .claude/worktrees)
    - grep -r "SWIPE_WINDOW" gesture_keys/ tests/ returns no matches (excluding .claude/worktrees)
    - grep "config.gestures" gesture_keys/pipeline.py returns no matches
    - grep "config.swipe_window" gesture_keys/pipeline.py returns no matches
    - grep "config.actions" gesture_keys/pipeline.py returns at least 1 match (threshold extraction)
    - grep "sequence_window=config.sequence_window" gesture_keys/pipeline.py returns 1 match
    - grep "len(config.actions)" gesture_keys/__main__.py returns 1 match
    - grep "actions loaded" gesture_keys/__main__.py returns 1 match
    - tests/test_pipeline.py passes
  </acceptance_criteria>
  <done>swipe.py, test_swipe.py, and test_integration_mutual_exclusion.py deleted. Pipeline uses config.actions for thresholds and config.sequence_window for orchestrator. __main__.py banner shows action count. No file imports from gesture_keys.swipe. DebounceState has no SWIPE_WINDOW.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy config code and clean test_config.py</name>
  <files>
    gesture_keys/config.py
    config.yaml
    tests/test_config.py
  </files>
  <read_first>
    gesture_keys/config.py
    config.yaml
    tests/test_config.py
  </read_first>
  <action>
1. **Edit `gesture_keys/config.py` -- Remove legacy AppConfig fields:**
   Remove these fields from the `AppConfig` dataclass (lines 302-331):
   - `gestures: dict[str, dict[str, Any]]` (line 309)
   - `swipe_enabled: bool` (line 313)
   - `swipe_cooldown: float` (line 314)
   - `swipe_min_velocity: float` (line 315)
   - `swipe_min_displacement: float` (line 316)
   - `swipe_axis_ratio: float` (line 317)
   - `swipe_mappings: dict[str, str]` (line 318)
   - `swipe_settling_frames: int` (line 319)
   - `swipe_window: float` (line 325)
   - `gesture_swipe_mappings: dict[str, dict[str, str]]` (line 326)

   **Rename and add:**
   - Rename nothing -- instead ADD `sequence_window: float = 0.5` to replace `swipe_window` (same default 0.2 changed to 0.5 to match config.yaml's detection.swipe_window value; actually keep default 0.2 and let load_config set it from YAML).
   - Actually: add `sequence_window: float = 0.2` to AppConfig to replace `swipe_window`.

   **Keep these fields** (still used by actions: path):
   - `gesture_cooldowns`, `gesture_modes`, `hold_release_delay`, `activation_gate_*`, `actions`

   Remove the `from typing import Any` import since `dict[str, dict[str, Any]]` (gestures field) was the only user. Check if `Any` is used elsewhere in the file first -- it is NOT used elsewhere, so remove it.

2. **Edit `gesture_keys/config.py` -- Delete legacy helper functions:**
   Delete these functions entirely:
   - `_extract_bypass_gestures()` (lines 374-379)
   - `_extract_gesture_cooldowns()` (lines 382-395)
   - `_extract_gesture_modes()` (lines 398-448)
   - `extract_gesture_swipe_mappings()` (lines 451-485)
   - `build_action_maps()` (lines 488-519)
   - `build_compound_action_maps()` (lines 522-546)
   - `resolve_hand_gestures()` (lines 549-562)
   - `resolve_hand_swipe_mappings()` (lines 565-580)

3. **Edit `gesture_keys/config.py` -- Simplify `load_config()`:**
   Remove the entire legacy `else:` branch (lines 686-721) that handles `gestures:/swipe:` config.
   Remove the mutual exclusion check (lines 618-625) that validates actions: vs gestures:/swipe: coexistence.
   Remove the `has_legacy` variable (line 620).
   Remove the check `if not has_actions and "gestures" not in raw:` (lines 628-631).

   The simplified load_config should:
   - Require `actions` section (raise ValueError if missing)
   - Parse `actions:` via `parse_actions()` and `derive_from_actions()`
   - Read `detection.swipe_window` and store as `sequence_window` (keep reading from `detection.swipe_window` in YAML for now since config.yaml has it as `swipe_window`, OR rename in config.yaml too -- rename in config.yaml to `sequence_window`)

   Actually, the simplest approach: in load_config, read `detection.sequence_window` with fallback to `detection.swipe_window` for backward compat, defaulting to 0.5:
   ```python
   sequence_window = float(
       detection.get("sequence_window", detection.get("swipe_window", 0.5))
   )
   ```

   Wait -- config.yaml currently has `swipe_window: 0.5` under `detection:`. Since we're cleaning up, rename it in config.yaml to `sequence_window: 0.5` and just read `detection.get("sequence_window", 0.5)` in load_config.

   The simplified `load_config` return should be:
   ```python
   if "actions" not in raw:
       raise ValueError(
           f"Config file {path} missing required 'actions' section"
       )

   action_entries = parse_actions(raw["actions"])
   derived = derive_from_actions(action_entries)

   activation_gate_bypass = list(
       set(activation_gate_bypass_cfg) | set(derived.activation_gate_bypass)
   )

   return AppConfig(
       camera_index=int(camera.get("index", 0)),
       smoothing_window=int(detection.get("smoothing_window", 2)),
       activation_delay=float(detection.get("activation_delay", 0.15)),
       cooldown_duration=float(detection.get("cooldown_duration", 0.3)),
       distance_enabled=bool(distance.get("enabled", False)),
       min_hand_size=float(distance.get("min_hand_size", 0.15)),
       max_hand_size=float(distance.get("max_hand_size", 0.0)),
       hold_release_delay=float(detection.get("hold_release_delay", 0.1)),
       hold_repeat_interval=float(detection.get("hold_repeat_interval", 0.03)),
       preferred_hand=preferred_hand,
       sequence_window=float(detection.get("sequence_window", 0.5)),
       activation_gate_enabled=activation_gate_enabled,
       activation_gate_gestures=activation_gate_gestures,
       activation_gate_duration=activation_gate_duration,
       gesture_modes=derived.gesture_modes,
       gesture_cooldowns=derived.gesture_cooldowns,
       activation_gate_bypass=activation_gate_bypass,
       actions=action_entries,
   )
   ```

4. **Edit `config.yaml`:**
   - Under `detection:`, rename `swipe_window: 0.5` to `sequence_window: 0.5` and update the comment from `# seconds to wait for static-to-swipe compound gesture` to `# seconds window for sequence gesture detection`.

5. **Edit `tests/test_config.py` -- Remove imports of deleted functions:**
   Remove from the import block (lines 8-21):
   - `_extract_gesture_modes`
   - `build_action_maps`
   - `build_compound_action_maps`
   - `extract_gesture_swipe_mappings`
   - `resolve_hand_gestures`

   Keep: `ActionEntry`, `AppConfig`, `ConfigWatcher`, `DerivedConfig`, `derive_from_actions`, `load_config`, `parse_actions`

6. **Edit `tests/test_config.py` -- Delete legacy test classes entirely:**
   Delete these classes:
   - `TestSwipeConfig` (lines 271-388) -- tests legacy swipe: config parsing
   - `TestSettlingFramesConfig` (lines 390-424) -- tests swipe_settling_frames
   - `TestGestureModesConfig` (lines 520-605) -- tests _extract_gesture_modes (legacy function)
   - `TestHandResolution` (lines 607-619) -- tests resolve_hand_gestures (legacy function)
   - `TestSwipeWindowConfig` (lines 622-710) -- tests swipe_window and gesture_swipe_mappings
   - `TestFireModeConfig` (lines 821-933) -- tests _extract_gesture_modes, build_action_maps, build_compound_action_maps, extract_gesture_swipe_mappings

7. **Edit `tests/test_config.py` -- Update tests that use legacy gestures: YAML:**
   - `TestLoadConfigCustomPath.test_loads_custom_config` (line 85): Rewrite YAML to use `actions:` format instead of `gestures:`. Change assertion from `config.gestures["open_palm"]["threshold"]` to checking `config.actions[0].name` or `len(config.actions)`.
   - `TestLoadConfigErrors.test_missing_gestures_and_actions_raises_value_error` (line 132): Update expected error -- should now raise ValueError matching "missing required 'actions' section".
   - `TestAppConfigTimingFields` (lines 142-190): Rewrite MINIMAL_YAML and TIMING_YAML to use `actions:` format. Update assertions to not reference gestures.
   - `TestDistanceConfig` (lines 192+): If any tests use `gestures:` YAML, rewrite to use `actions:`.
   - `TestGestureCooldownsConfig` (lines 426-477): Tests that use legacy `gestures:` format with `cooldown:` field -- delete the entire class since per-action cooldowns are already tested in `TestLoadConfigActions.test_actions_derives_cooldowns`.
   - `TestPreferredHandConfig` (lines 479-517): Rewrite MINIMAL_YAML to use `actions:` format.
   - `TestLoadConfigActions.test_mutual_exclusion_raises` and `test_mutual_exclusion_with_swipe_raises` (lines 767-793): Delete both tests since the mutual exclusion logic is being removed.
   - `TestLoadConfigActions.test_fallback_to_gestures_when_no_actions` (line 795): Delete -- no more fallback.
   - `TestLoadConfigActions.test_actions_path_gestures_empty` (line 807): Delete -- no more gestures field.

8. **Update `tests/test_config.py` -- swipe_window -> sequence_window:**
   - In `TestLoadConfigDefault`, add a test: `test_sequence_window_default` that asserts `config.sequence_window == 0.5` from the default config.yaml.
   - Any test that previously asserted `config.swipe_window` should use `config.sequence_window`.
  </action>
  <verify>
    <automated>cd C:/Users/wsenr/repos/source/gesture-keys && python -m pytest tests/test_config.py tests/test_pipeline.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "swipe_enabled\|swipe_cooldown\|swipe_min_velocity\|swipe_min_displacement\|swipe_axis_ratio\|swipe_mappings\|swipe_settling_frames\|swipe_window\|gesture_swipe_mappings" gesture_keys/config.py returns no matches
    - grep "def _extract_bypass_gestures\|def _extract_gesture_cooldowns\|def _extract_gesture_modes\|def extract_gesture_swipe_mappings\|def build_action_maps\|def build_compound_action_maps\|def resolve_hand_gestures\|def resolve_hand_swipe_mappings" gesture_keys/config.py returns no matches
    - grep "gestures:" gesture_keys/config.py returns no matches (no legacy field reference)
    - grep "sequence_window" gesture_keys/config.py returns at least 2 matches (field definition + load_config usage)
    - grep "sequence_window: 0.5" config.yaml returns 1 match
    - grep "gestures:\|swipe:" config.yaml returns no matches
    - grep "swipe_window" config.yaml returns no matches
    - grep "extract_gesture_swipe\|build_action_maps\|build_compound\|resolve_hand_gestures\|_extract_gesture_modes" tests/test_config.py returns no matches
    - grep "class TestSwipeConfig\|class TestSettlingFrames\|class TestSwipeWindowConfig\|class TestFireModeConfig\|class TestHandResolution\|class TestGestureModesConfig" tests/test_config.py returns no matches
    - grep "config.gestures" tests/test_config.py returns no matches
    - python -m pytest tests/test_config.py -x passes
    - python -m pytest tests/ -x passes (full suite)
  </acceptance_criteria>
  <done>AppConfig has no swipe fields (uses sequence_window instead of swipe_window). All legacy helper functions deleted. load_config only supports actions: format. config.yaml uses sequence_window. test_config.py has no legacy test classes or imports. Full test suite passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m pytest tests/ -x -q` -- all tests pass
2. `grep -r "from gesture_keys.swipe" gesture_keys/ tests/` -- no matches (exclude .claude/worktrees)
3. `grep -r "swipe" gesture_keys/config.py` -- no matches
4. `grep -r "gestures:" config.yaml` -- no matches
5. `python -c "from gesture_keys.config import load_config; c = load_config('config.yaml'); print(f'OK: {len(c.actions)} actions, seq_window={c.sequence_window}')"` -- prints "OK: 11 actions, seq_window=0.5"
</verification>

<success_criteria>
- swipe.py and test_swipe.py deleted, no remaining imports
- config.yaml uses actions: format with sequence_window, no gestures:/swipe: sections
- config.py has no legacy functions, no swipe AppConfig fields, no gestures:/swipe: parsing path
- All tests pass (pytest tests/ -x exits 0)
- Application starts correctly: `python -c "from gesture_keys.config import load_config; load_config('config.yaml')"` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/24-cleanup-and-config-migration/24-01-SUMMARY.md`
</output>
