---
phase: 21-orchestrator-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gesture_keys/orchestrator.py
  - tests/test_orchestrator.py
autonomous: true
requirements:
  - ORCH-03

must_haves:
  truths:
    - "SWIPE_WINDOW, SWIPING, and COMPOUND_FIRE no longer exist in orchestrator.py"
    - "All non-swipe lifecycle tests still pass (tap, hold, cooldown, direct transitions)"
    - "update() no longer accepts swipe_direction or swiping parameters"
    - "OrchestratorResult no longer has suppress_standalone_swipe field"
  artifacts:
    - path: "gesture_keys/orchestrator.py"
      provides: "Orchestrator with swipe code stripped"
      contains: "class GestureOrchestrator"
    - path: "tests/test_orchestrator.py"
      provides: "Tests with swipe tests deleted and remaining tests passing"
      contains: "class TestOrchestratorStateTransitions"
  key_links:
    - from: "gesture_keys/orchestrator.py"
      to: "gesture_keys/trigger.py"
      via: "Direction import replacing SwipeDirection"
      pattern: "from gesture_keys\\.trigger import Direction"
---

<objective>
Strip all swipe-related code from the orchestrator FSM and its tests, leaving a simplified lifecycle (IDLE -> ACTIVATING -> ACTIVE -> COOLDOWN) with only tap and hold modes.

Purpose: Clean foundation for adding MOVING_FIRE and SEQUENCE_FIRE signals in Plan 02 without swipe code interference.
Output: Simplified orchestrator.py and updated test_orchestrator.py with all swipe tests removed.
</objective>

<execution_context>
@C:/Users/wsenr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wsenr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-orchestrator-refactor/21-CONTEXT.md
@.planning/phases/21-orchestrator-refactor/21-RESEARCH.md

@gesture_keys/orchestrator.py
@tests/test_orchestrator.py
@gesture_keys/trigger.py

<interfaces>
<!-- Key types the executor needs from trigger.py -->

From gesture_keys/trigger.py:
```python
class Direction(Enum):
    LEFT = "left"
    RIGHT = "right"
    UP = "up"
    DOWN = "down"
```

From gesture_keys/classifier.py:
```python
class Gesture(Enum):
    # Various gesture values
```

Current orchestrator.py imports to change:
```python
# REMOVE: from gesture_keys.swipe import SwipeDirection
# ADD: from gesture_keys.trigger import Direction
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip swipe code from orchestrator.py</name>
  <files>gesture_keys/orchestrator.py</files>
  <action>
Systematically remove all swipe-related code from orchestrator.py following this checklist (from RESEARCH.md Pattern 3):

1. **Import**: Remove `from gesture_keys.swipe import SwipeDirection`. Add `from gesture_keys.trigger import Direction` (will be used in Plan 02, but import now to avoid SwipeDirection references).

2. **Enums**:
   - Delete `COMPOUND_FIRE` from OrchestratorAction
   - Delete `SWIPE_WINDOW` from LifecycleState
   - Delete `SWIPING` from TemporalState

3. **OrchestratorSignal**: Change `direction: Optional[SwipeDirection] = None` to `direction: Optional[Direction] = None`

4. **OrchestratorResult**: Delete `suppress_standalone_swipe: bool = False` field entirely

5. **Constructor**:
   - Remove `swipe_gesture_directions` parameter and `_swipe_gesture_directions` field
   - Remove `swipe_window` parameter and `_swipe_window` field
   - Remove instance vars: `_swipe_window_start`, `_suppress_until`, `_was_swiping`, `_pre_swipe_gesture`

6. **Methods to DELETE entirely**:
   - `_handle_swiping_transitions()` -- the whole method
   - `_handle_swipe_window()` -- the whole method

7. **update() method**:
   - Remove `swipe_direction` and `swiping` keyword parameters
   - Remove the `_handle_swiping_transitions()` call and early return
   - Remove the `SWIPE_WINDOW` dispatch branch
   - Remove `self._was_swiping = swiping` line

8. **_handle_idle()**:
   - Remove the `if gesture.value in self._swipe_gesture_directions` branch
   - Simplify: gesture is not None -> always go ACTIVATING (per RESEARCH.md simplified code)

9. **_handle_active()**:
   - Remove the `elif self._temporal_state == TemporalState.SWIPING` branch

10. **_handle_cooldown()**:
    - Remove the `if gesture.value in self._swipe_gesture_directions` branch for SWIPE_WINDOW routing
    - Simplify: different gesture during cooldown -> always go ACTIVATING

11. **_build_result()**:
    - Remove `SWIPE_WINDOW` branch for base_gesture
    - Remove `suppress = timestamp < self._suppress_until` line
    - Remove `suppress_standalone_swipe=suppress` from return

12. **reset()**:
    - Remove `_swipe_window_start`, `_suppress_until`, `_was_swiping`, `_pre_swipe_gesture` resets

13. **Properties**:
    - Delete `in_swipe_window` property entirely
    - Update `activating_gesture` docstring (remove "or in swipe window")

14. **flush_pending()**:
    - Simplify to always return empty OrchestratorResult (no SWIPE_WINDOW check needed). Keep the method as pipeline.py calls it. Body becomes: `return OrchestratorResult(outer_state=self._outer_state)`

15. **Module docstring**: Update to remove swipe references. New outer FSM: IDLE -> ACTIVATING -> ACTIVE -> COOLDOWN. Inner: CONFIRMED, HOLD.

After all removals, grep for "swipe" (case-insensitive) in orchestrator.py -- should return zero hits.
  </action>
  <verify>
    <automated>cd C:/Users/wsenr/repos/source/gesture-keys && python -c "import gesture_keys.orchestrator; print('import OK')" && python -c "
import re
with open('gesture_keys/orchestrator.py') as f:
    content = f.read()
matches = re.findall(r'(?i)swipe', content)
assert len(matches) == 0, f'Found {len(matches)} swipe references: {matches}'
print('No swipe references found - PASS')
"</automated>
  </verify>
  <done>orchestrator.py has zero swipe references, imports Direction from trigger.py, has 4 lifecycle states (IDLE, ACTIVATING, ACTIVE, COOLDOWN), 2 temporal states (CONFIRMED, HOLD), 3 actions (FIRE, HOLD_START, HOLD_END), and OrchestratorResult has no suppress_standalone_swipe field</done>
</task>

<task type="auto">
  <name>Task 2: Update test_orchestrator.py -- delete swipe tests, fix remaining</name>
  <files>tests/test_orchestrator.py</files>
  <action>
Update the test file to match the stripped orchestrator:

1. **Imports**: Remove `from gesture_keys.swipe import SwipeDirection`. No replacement needed yet (Direction tests come in Plan 02).

2. **DELETE entire test classes**:
   - `TestSwipeWindow` (class starting at line ~458)
   - `TestSwipingTransitions` (class starting at line ~730)
   - `TestCompoundSwipeSuppression` (class starting at line ~773)

3. **DELETE specific test methods from remaining classes**:
   - `TestTypeDefinitions.test_orchestrator_signal_with_direction` (references SwipeDirection and COMPOUND_FIRE)
   - `TestTypeDefinitions.test_orchestrator_signal_unpacking` (uses 3-field tuple unpacking -- will be replaced in Plan 02)
   - `TestProperties.test_in_swipe_window_false_by_default`
   - `TestProperties.test_in_swipe_window_true_during_swipe_window`
   - `TestProperties.test_is_activating_false_during_swipe_window`
   - `TestProperties.test_activating_gesture_during_swipe_window`
   - `TestTemporalStateInvariants.test_temporal_state_none_in_swipe_window`
   - `TestBaseGesture.test_base_gesture_set_during_swipe_window`
   - `TestFlushPending.test_flush_pending_fires_in_swipe_window` (flush_pending is now always a no-op)
   - `TestEdgeCases.test_edge_1_direct_transition_to_swipe_window`
   - `TestEdgeCases.test_edge_3_swipe_exit_reset`
   - `TestEdgeCases.test_edge_4_pre_swipe_gesture_suppression`
   - `TestEdgeCases.test_edge_5_flush_pending` (references SWIPE_WINDOW)
   - `TestEdgeCases.test_edge_8_compound_swipe_suppression`

4. **MODIFY test methods**:
   - `TestTypeDefinitions.test_orchestrator_action_values`: Remove COMPOUND_FIRE assertion
   - `TestTypeDefinitions.test_lifecycle_state_values`: Remove SWIPE_WINDOW assertion
   - `TestTypeDefinitions.test_temporal_state_values`: Remove SWIPING assertion
   - `TestTypeDefinitions.test_orchestrator_result_defaults`: Remove `assert result.suppress_standalone_swipe is False`
   - `TestConstructor.test_accepts_all_config_params`: Remove `swipe_gesture_directions` and `swipe_window` params. Keep other params unchanged.
   - `TestDirectTransitions`: If any test in this class uses `swipe_gesture_directions`, update to remove it. The `test_different_gesture_during_cooldown_starts_activating` should still work since it tests non-swipe direct transitions.
   - `TestReset.test_reset_returns_to_idle` and other reset tests: If they reference swipe state, simplify.

5. **Verify zero swipe references**: Grep test file for "swipe" (case-insensitive) -- should find zero hits.

6. **Update test docstring** at top of file to remove swipe references.

After modifications, run `python -m pytest tests/test_orchestrator.py -x -q` -- all remaining tests must pass.
  </action>
  <verify>
    <automated>cd C:/Users/wsenr/repos/source/gesture-keys && python -m pytest tests/test_orchestrator.py -x -q</automated>
  </verify>
  <done>All remaining orchestrator tests pass, no swipe references in test file, deleted classes/methods are gone, modified assertions match new enum values</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_orchestrator.py -x -q` -- all remaining tests pass
2. `python -c "import re; content=open('gesture_keys/orchestrator.py').read(); assert not re.search(r'(?i)swipe', content)"` -- zero swipe refs in production
3. `python -c "import re; content=open('tests/test_orchestrator.py').read(); assert not re.search(r'(?i)swipe', content)"` -- zero swipe refs in tests
4. `python -m pytest tests/ -x -q` -- full test suite passes (no other file broken by removal)
</verification>

<success_criteria>
- Orchestrator FSM has 4 lifecycle states (no SWIPE_WINDOW), 2 temporal states (no SWIPING), 3 actions (no COMPOUND_FIRE)
- update() accepts only gesture and timestamp (no swipe_direction, no swiping)
- OrchestratorResult has no suppress_standalone_swipe
- All non-swipe tests pass unchanged
- Zero "swipe" references in orchestrator.py and test_orchestrator.py
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/21-orchestrator-refactor/21-01-SUMMARY.md`
</output>
